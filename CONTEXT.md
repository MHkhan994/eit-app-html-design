# ETI-App — Project Context

> Tech stack, design system, architecture, and standing development rules now live in
> [`CLAUDE.md`](./CLAUDE.md). This file is the screen-by-screen build history/log — what's been
> built, per-screen decisions and evidence notes, what's left, and known gaps.

## What this is
A Bangladeshi poultry farm management app being rebuilt for a client: **functionality copy of an existing app, with upgraded visual design.** Client provided the existing app to clone; I'm rebuilding it screen-by-screen from screen recordings, feature by feature.

Core features the full app needs (from client brief): Buy/Sell marketplace, doctor/vet appointment booking, blog, general poultry data management. The Home screen itself has three tabs: **General**, **Parent Stock**, **Hatchery**.

## Workflow being used
1. User sends either a screen recording (via phone) of one feature/screen, **or** (from 2026-08-03 onward) a folder path containing pre-extracted screenshots for one feature/screen.
2. If it's a video, I extract frames (ffmpeg) and identify distinct screens/states. If it's an image folder, I read the images directly — no extraction step needed.
3. I build a **functionality-match** version first (matches structure/content of the original).
4. User reviews, then I do an **upgraded design pass** applying our design system.
5. Repeat per feature until the whole app is covered.

**Note on process history:** We started in Figma (via Figma MCP) but hit the Starter plan's MCP tool call rate limit partway through the upgraded design pass on the Home screen. Switched to building directly as HTML/CSS mockups instead — free, no rate limits, and the user can host and demo it directly. (See `CLAUDE.md` for the tech stack, design system, architecture, and standing rules this settled into.)

## Screens built so far
1. **Home / General tab** — banner carousel, date/location info strip, notice ticker, market price list (chip-style), 6-item icon grid menu (Accounting, Production Cost, Farm Management, Vaccine Info, Disease Diagnosis, Disease Info). ✅ Functionality-match + upgraded design both done.
2. **Home / Parent Stock tab** — banner carousel, 6-item icon grid (Broiler PS Guide, Color Chicken PS, Brown Layer PS, White Layer PS, Duck PS, All STD Data). ✅ Functionality-match + upgraded done. Note: used emoji placeholders for breed illustrations — real source images not yet provided.
3. **Home / Hatchery tab** — banner carousel (gold gradient, 3 dots), 6-item icon grid (Troubleshooting, Fine Tuning, Embryonic Stages, Important Topics, Management, Pull-Out Check). ✅ Functionality-match built (matches structure from `WhatsApp Video 2026-08-02 at 8.15.04 PM.mp4`). Note: original app uses custom illustrated icons + a real photo banner (chick tray); rebuilt with badge-circle/emoji treatment per design system since no real assets provided yet. Tapping grid items goes nowhere yet — sub-screens not recorded.
4. **Bottom nav shell + top tab shell** — real multi-page navigation between Home/Buy/Sell/Doctor/Data Bank (bottom nav, one HTML file each, in their own folders) and General/Parent Stock/Hatchery (top tabs, JS-toggled subpanels within `index.html`). Unbuilt sections show a "Not yet built" placeholder card rather than breaking.
5. **General / হিসাব নিকাশ (Accounting) drill-down** — `accounting/index.html`. Rust-colored sub-page header with back arrow (no bottom nav, matches recording), vertical list of 10 calculator tools (weight/uniformity, ROI/FCR, egg mass, breeder feed program, temp converter, layer egg production %, medicine dosage, space/feeder/drinker calc, chick quality check, standard data by age). ✅ Functionality-match built (matches structure from `WhatsApp Video 2026-08-02 at 8.41.12 PM.mp4`).
6. **মুরগীর ওজন ও ইউনিফির্মিটি (weight/uniformity) calculator** — `accounting/weight-uniformity.html`, linked from the first accounting list item. **Static visual mockup only — deliberately not wired up.** User explicitly said not to build working calculator logic for these tool screens, just replicate the visual states (matches structure from `WhatsApp Video 2026-08-02 at 8.44.43 PM.mp4`, default empty/zeroed state shown). Same pattern applies to the other 9 accounting tools and any future calculator/tool screens: **build the static UI, skip the actual computation.**
7. **ROI/FCR calculator** — `accounting/roi-fcr.html`, linked from the second accounting list item. Static mockup (matches structure from `WhatsApp Video 2026-08-02 at 8.54.27 PM.mp4`). Note: unlike weight-uniformity, this screen has **no colored header bar** in the original — just a plain background with a bold green title (new `.plain-header` component) and a **working** Broiler/Color bird vs Layer radio toggle above the form. The radio toggle actually switches which field set is shown (Broiler: 7 fields ending in Market price/kg; Layer: 4 fields — Daily feed (gm), Hen day %, Egg price, Feed price/kg) via a new generic `showFieldGroup(id)` helper in `app.js` + `.field-group`/`.field-group.active` CSS. **This is a visual state swap only, not a calculation** — still no working math, per the "static mockup" rule, but swapping which inputs are visible on radio-select is fair game since it's just showing another recorded UI state, not computing a result. Single CALCULATE button, no result panel shown in the default state (original only reveals results after tapping Calculate).

8. **এগ ম্যাস (Egg Mass) calculator** — `accounting/egg-mass.html`, linked from the third accounting list item. Static mockup, default empty state (matches structure from `WhatsApp Video 2026-08-02 at 9.00.04 PM.mp4`). Uses the rust `.subpage-header` (like weight-uniformity). New pattern here: **labeled fields** (bold label above each input box, via new `.calc-field` class) rather than placeholder-only inputs, a **two-button row** (`.calc-btn-row` — ফলাফল জানুন + মুছুন side by side instead of stacked), and a second results card using a new `.info-block`/`.info-row` component (stacked label + blank value, for the বর্তমান Egg Mass / স্ট্যান্ডার্ড সীমা / ফ্লকের বর্তমান অবস্থা / সংক্ষিপ্ত পরামর্শ summary — all blank by default since the original only fills them in live as you type, which we don't replicate). Note: the source recording briefly caught an unrelated WhatsApp call-notification popup mid-video — ignored, not part of the app UI.

9. **ব্রিডার ফার্মে স্কিপ ফিড প্রোগ্রাম (breeder skip-feed program) calculator** — `accounting/breeder-feed.html`, linked from the fourth accounting list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 9.13.54 PM.mp4`). Plain header (no colored bar, like ROI/FCR). New pattern: a **vertical radio list** (new `.radio-col`/`.field-section-label` classes) for choosing between 4 named programs (৭/৭, ৬/৭, ৫/৭, ৪/৭ প্রোগ্রাম) — distinct from the ROI screen's horizontal 2-option `.radio-row`. CALCULATE (moss) + CLEAR (rust) side by side via `.calc-btn-row`, and an empty `.info-block` placeholder below (original shows a genuinely blank result card until Calculate is tapped — no labels pre-rendered here, unlike egg-mass which does show its labels by default). Note: original renders the "স্কিপ ফিড প্রোগ্রাম নির্বাচন করুন" section label in very low-contrast/near-invisible text — not replicated, given a normal readable label instead since that's very likely an unintentional contrast bug in the source app, not a deliberate design choice worth cloning.

10. **তাপমাত্রা পরিবর্তক (Temperature Unit Converter)** — `accounting/temp-converter.html`, linked from the fifth accounting list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 9.18.01 PM.mp4` — note the user first pasted the *breeder-feed* video's filename again by mistake for this screen; caught it by checking the file timestamp against what was already processed, and asked for the correct one before building anything). Uses `.subpage-header` but in **moss**, not rust (original header is teal-green, English title "Temperature Unit Converter"). New patterns: the Celsius input has a distinct thick rust-colored border even at rest (new `.calc-input.accent`, not just a focus ring — both fields render this way with/without focus in the recording) and a `.calc-btn.dark` variant (mapped to `--text-charcoal`, standing in for the original's slate-gray CLEAR button since gray isn't one of our tokens). Two convert buttons (moss "→ Fahrenheit", rust "→ Celsius") plus CLEAR; no result bar shown by default (original only reveals `40°C = 104°F`-style output after tapping Convert).

11. **লেয়ার মুরগির ডিম উৎপাদন হার (%) (layer egg production rate) calculator** — `accounting/layer-egg-production.html`, linked from the sixth accounting list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 9.25.50 PM.mp4`). Rust `.subpage-header`, 3 placeholder-only fields, `.calc-btn-row` (rust "হিসাব করুন" + `.calc-btn.dark` "মুছুন" — same dark/neutral button color as temp-converter's CLEAR). Two `.info-block` cards stacked: first has **live default values shown as "0%"** (not blank — new `.info-value.strong` variant, bold/larger) for ডিম উৎপাদন হার and স্ট্যান্ডার্ড উৎপাদন হার, plus a **moss-green label** (new `.info-label.moss` modifier) for ফ্লকের বর্তমান অবস্থা with a blank value; second card is a lone সংক্ষিপ্ত পরামর্শ row, blank. Another instance of "match what THIS screen's default state actually shows" — some tool screens default their numeric results to 0% (this one, matches weight-uniformity), others leave everything blank until Calculate (ROI/FCR, breeder-feed, temp-converter) — always check the recording rather than assuming one convention for all.

12. **মেডিসিনের মাত্রা (medicine dosage) calculator** — `accounting/medicine-dosage.html`, linked from the seventh accounting list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 9.28.47 PM.mp4`). Rust `.subpage-header`, 4 `.calc-field` labeled inputs (মোরগ মুরগির সংখ্যা, গড় ওজন (g), ডোজ/কেজি, মেডিসিন/গ্রাম), rust+dark `.calc-btn-row`. Result card is the simplest yet — just a single line, "Result will appear here" (English placeholder text, in the original too), no label/value rows at all.

13. **জায়গা, ফিডার ও ড্রিংকার (space/feeder/drinker) calculator** — `accounting/space-feeder-drinker.html`, linked from the eighth accounting list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 9.42.48 PM (1).mp4`). Moss `.subpage-header`. New pattern: a **dropdown/`<select>`** for chicken type (ব্রয়লার / লেয়ার-কালার বার্ড/পুলেট / ব্রয়লার ব্রিডার, defaulting to the first option), styled with the existing `.calc-input` class. Also: unlike the other tool screens, **fields sit directly on the background with no big white `.calc-card` wrapper** — just a plain padded div — match whichever this particular recording actually uses, don't assume every tool screen has the white card. Rust/dark `.calc-btn-row`, same "Result will appear here" placeholder card as medicine-dosage. Note: the recording's thick-orange-border look on the number inputs turned out to be a **transient focus ring** (confirmed by comparing focused vs. unfocused frames of the same field), not a persistent style like temp-converter's Celsius field — so plain default borders were used here, not `.calc-input.accent`. Always check focused vs. unfocused frames before deciding whether a visual is a resting style or just focus state.

14. **চিক Quality Check (Chick Quality Calculator)** — `accounting/chick-quality.html`, linked from the ninth accounting list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 9.57.47 PM.mp4`). **Most different layout of all 9 tool screens so far**: no colored header bar at all — not even the plain-header title text, just a bare back arrow (`.plain-header` with no `h1`) — and the content is three stacked white cards instead of one form card: a new `.title-card` (🐤 emoji + "Chick Quality Calculator" heading + gray subtitle), then the form card (`.calc-card`, dropdown for parent breed defaulting to ব্রয়লার — ব্রয়লার/কালার বার্ড/লেয়ার — plus 2 labeled fields), then the `.info-block` result placeholder. Buttons are **stacked, not side-by-side** (plain `.calc-btn.moss` + `.calc-btn.dark`, no `.calc-btn-row`). Original's Calculate button is rendered **bright blue** — mapped to moss instead of introducing a new color token, consistent with how temp-converter's slate CLEAR button was mapped to `--text-charcoal`. This confirms tool screens in this app are visually inconsistent with each other in the original (different header styles, button layouts, and now even a stray color) — always look at what THIS screen's own recording shows rather than assuming a shared template.

**Update (2026-08-02, later same day):** user asked to replace every empty/"Result will appear here"/blank-value result state across all 9 built calculators with an actual example result — **"it's for UI design, every element should be in UI, don't worry about logic."** All 9 calculator screens now show filled example output (real captured values where the recording showed a computed result — weight-uniformity, medicine-dosage, space-feeder-drinker, chick-quality; plausible synthesized values where the recording ended before Calculate was pressed or only showed a placeholder emoji — ROI/FCR, breeder-feed, temp-converter, layer-egg-production). This **supersedes** the "match the recorded default/empty state" notes in the per-screen entries above for the *results* sections specifically — form inputs above the results are still left as empty placeholders (matching the "don't worry about logic" instruction: only the output display needed to look complete, not the input state). Two new shared components came out of this: `.result-banner` (single-line centered result, e.g. temp-converter's "40°C = 104°F") and `.result-text` (multi-paragraph free-form result blocks, e.g. space-feeder-drinker and chick-quality's long emoji-bulleted output). **Going forward, every new calculator/tool screen should ship with its result section pre-filled with realistic example data, not a blank/placeholder state** — this is now the standing convention, not a one-off.

15. **বয়স অনুযায়ী মুরগির স্ট্যান্ডার্ড ডাটা (standard data by age)** — `accounting/standard-data.html`, linked from the tenth (final) accounting list item. Matches structure from `WhatsApp Video 2026-08-02 at 10.15.01 PM.mp4` (only 4 seconds — just shows this one list appearing, no deeper screen). **Structurally different from the other 9 accounting tools**: this is a **sub-list/menu screen** ("Commercial Standard Data" rust header + `.list-wrap`/`.list-card`, reusing the exact same component as `accounting/index.html` itself), not a form-plus-result calculator. 4 breed options — Broiler / Color / Layer / Duck Commercial Standard Data — each with the **identical** English description text ("Please select the breed, put the age in week and click for standard data.") in the original, which reads like a copy-paste artifact rather than intentional, but was replicated as-is since that's genuinely what's recorded. **All 10 accounting tools are now built.**

15a. **The 4 breed sub-screens behind standard-data.html** — `accounting/standard-data-broiler.html`, `standard-data-color.html`, `standard-data-layer.html`, `standard-data-duck.html`, matching structure from `WhatsApp Video 2026-08-02 at 10.31.46 PM.mp4`. All 4 share the header-less `.plain-header` (bare back arrow) + `.title-card` + `.calc-card` + result skeleton from `chick-quality.html` — reused with zero new CSS. Per-screen specifics (each genuinely different, read off the frames, not copied):
    - **Broiler**: breed dropdown Indian River (IR)/Ross 308/Hubbard Efficiency Plus (EP)/Siam Broiler/Cobb 500. Result: "📊 Standard Data" — Body Weight, Cumulative Feed Intake, FCR.
    - **Color**: only one confirmed breed option, **Sasso Color (SA51A)** — the dropdown was never opened in the recording, so no other options were invented. Same "📊 Standard Data" result shape as Broiler.
    - **Layer**: breed dropdown Shaver Brown/Hi-sex White/Hy-Line Brown/Hy-Line White/Novogen Brown/Novogen White/Dekalb White. Result heading is **"📊 Rearing Standard Data"** (not "Standard Data") with a different metric set — Body Weight, Daily Feed, Cumulative Feed (no FCR).
    - **Duck**: the outlier of the four — **Bengali button labels** ("স্ট্যান্ডার্ড দেখুন"/"মুছুন" instead of English "Calculate"/"Clear"), and the result sits inside its own **separate "ফলাফল" heading + bordered inner box**, not a plain `.info-block` like the other three. Breed dropdown Pekin Duck/Muscovy Duck/Cherry Valley Duck — Pekin wasn't seen selected in the recording but is named explicitly in the screen's own subtitle ("Pekin, Muscovy ও Cherry Valley..."), so including it is evidence-based, not invented. Result: same "📊 Standard Data" shape as Broiler/Color (Body Weight, Cumulative Feed Intake, FCR).
    
    All 4 breed cards in `standard-data.html` are now wired up (`<a class="list-card">`) to these pages.

16. **উৎপাদন খরচ জানুন (Production Cost) list** — `production-cost/index.html`, linked from the Home/General grid's উৎপাদন খরচ card. ✅ Functionality-match built (matches structure from `WhatsApp Video 2026-08-02 at 21.47.42.mp4`). Rust `.subpage-header`, no bottom nav, three `.list-card` entries: এক কেজি মুরগীর উৎপাদন খরচ (bachcha price/feed price/target weight/target FCR), এক দিনের মুরগীর বাচ্চার উৎপাদন খরচ (parent stock chick price/feed price/HH Chicks), একটি ডিমের উৎপাদন খরচ (layer chick price/feed price/HH egg). **Zero new CSS needed** — reused `.list-card`/`.list-title`/`.list-desc`/`.badge-circle.sm` from the accounting list wholesale; first screen built with no new components at all, which is the sign the design system is now covering this screen shape. Given its own folder (not a flat file) following the `accounting/` precedent, since all three items clearly drill down into input-form calculators ("...লিখুন এবং ফলাফলের জন্য ক্লিক করুন"). Cards are plain `<div>`s, not `<a>`s — those three sub-screens aren't recorded yet (recording ends on a tap ripple over card 2, before navigation). Two deliberate deviations from the original: it uses **circular emoji badges (🐔/🐤/🥚) instead of the original's real product photos** (chicken, chick group, egg basket), per the standing no-real-assets rule; and the original header has **no back arrow** (centered title only, relying on Android's system back), but one was added here since a hosted HTML demo has no equivalent affordance and every other sub-page has one.

17. **১ কেজি উৎপাদন খরচ হিসাব (per-kg production cost) calculator** — `production-cost/one-kg-cost.html`, linked from the first production-cost list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 22.00.10.mp4`). Three new components, all now in the shared CSS:
    - **`.inset-header`** — a *rounded rust block set in from the screen edges* rather than a full-width bar. This is the **third** header pattern in the app, alongside `.subpage-header` (full-width colored bar) and `.plain-header` (no bar at all) — check which one a recording actually shows before picking.
    - **`.calc-select`** — dropdown field: same box as `.calc-input` plus an SVG-data-URI caret. Rendered as a real `<select>`, left enabled because opening it reproduces a genuinely recorded state (the breed menu: ব্রয়লার / কালার বার্ড / সোনালি) rather than computing anything. Native select chrome replaces the original's Android overlay menu — same content, no JS.
    - **`.result-hero`** — centered small caption over one big bold moss-green number, for tools whose entire output is a single value.
    Fields, in order: breed `<select>`, বাচ্চার দাম (টাকা/বাচ্চা), ফিডের দাম (টাকা/কেজি), টার্গেট ওজন (কেজি), then a **pre-filled `1.6`** field, then a full-width rust হিসাব করুন button **inside** the same white `.calc-card` (not in a separate button row outside it, unlike the accounting tools). Result card below reads আনুমানিক উৎপাদন খরচ / **0 টাকা/কেজি** — another default-to-zero screen. Note: the `1.6` field's placeholder is never visible in the recording since the value covers it; labelled `টার্গেট FCR` here because the parent list card names target FCR as the fourth input. Recording ends on a tap ripple over the first input, so **no typed or computed state was ever captured** — nothing was invented to fill that gap.

18. **১ দিনের বাচ্চার উৎপাদন খরচ (day-old chick production cost) calculator** — `production-cost/day-old-chick-cost.html`, linked from the second production-cost list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 22.08.22.mp4`). **Zero new CSS** — same skeleton as `one-kg-cost.html` (`.inset-header` + `.calc-card` with the button inside it + `.result-hero`), which is what those three components were extracted for. Fields: breed `<select>`, প্যারেন্টস বাচ্চার দাম, ফিডের দাম (টাকা/কেজি), মোট HH বাচ্চার সংখ্যা — no pre-filled field here. Button reads **ফলাফল জানতে ক্লিক করুন**, not হিসাব করুন, and the result unit is **টাকা/বাচ্চা**, not টাকা/কেজি. **Its breed dropdown is NOT the same list as one-kg-cost's** — four options here (ব্রয়লার / কালার / সোনালি / **লেয়ার**) vs. three there (ব্রয়লার / **কালার বার্ড** / সোনালি); note both the extra লেয়ার entry and কালার vs কালার বার্ড. Sibling screens that look identical still differ in their option lists, button labels, and result units — read them off the frames per screen instead of copying the previous file's. No typed or computed state in the recording; result stays 0.
    - Process note: with 30 frames of a near-static screen, sampling every 6th frame missed the dropdown-open state entirely. `stat -f "%N %z" f_*.jpg` and looking for the file-size outliers found it immediately (frames 22–24 dipped ~10KB below the rest because the white menu covers detail) — cheaper than reading images one by one, and worth doing whenever a recording looks like one unchanging screen.

19. **১টি ডিমের উৎপাদন খরচ (per-egg production cost) calculator** — `production-cost/egg-cost.html`, linked from the third production-cost list item. Static mockup, default state (matches structure from `WhatsApp Video 2026-08-02 at 22.14.54.mp4`). Zero new CSS. **No breed dropdown at all** on this one (the screen is layer-specific), just three inputs — লেয়ার বাচ্চার দাম, ফিডের দাম (টাকা/কেজি), মোট ডিমের সংখ্যা (HH) — then a rust **হিসাব করুন** button and a **0 টাকা/ডিম** result. ✅ **This completes all three production-cost tools.** Taken together they're the clearest case yet of near-identical siblings differing in the details: same `.inset-header`/`.calc-card`/`.result-hero` skeleton every time, but the field sets, the presence of a breed `<select>` (3 options / 4 options / none), the button label (হিসাব করুন / ফলাফল জানতে ক্লিক করুন / হিসাব করুন) and the result unit (টাকা/কেজি / টাকা/বাচ্চা / টাকা/ডিম) are all different. Never clone a sibling file and assume — read each recording.

20. **খামার ব্যবস্থাপনা / ভ্যাকসিন তথ্য / রোগ নির্ণয় / রোগ বালাই — article list + detail template** — `farm-management/`, `vaccine-info/`, `disease-diagnosis/`, `disease-control/`, linked from the Home/General grid's remaining 4 cards. Matches structure from `WhatsApp Video 2026-08-02 at 10.58.09 PM.mp4`. User explicitly asked for a **reusable design** since all 4 sections share the same shape: rust `.subpage-header` + scrollable article list (thumbnail badge + bold title + truncated 2-line description, English all-caps subtitle line on 3 of the 4 — `.list-sub`, new) reusing `.list-wrap`/`.list-card`/`.badge-circle.sm` wholesale, and a shared **article detail template** (new `.article-hero`/`.article-hero-badge` — big emoji-in-circle standing in for the original's real infographic photos, per the standing no-real-assets rule; `.article-body`/`.article-h`/`.article-divider` for the long-form bullet-and-heading body text; the existing `.notice` component reused as-is for the closing 💡/✅ tip box). Only **one article per section got a full detail page** — matches what the recording actually tapped into and read through (the other's tap-throughs were never recorded, they went list → back → next grid card):
    - **খামার ব্যবস্থাপনা** (rust header): 6-item list (খামারের জৈব নিরাপত্তা, **শেড পরিষ্কার এবং জীবাণুমুক্তকরণ** ← linked, শেড ফিউমিগেশন এবং ব্রুডিং হাউস তৈরি, ব্রুডিং এর সময় করণীয়, লাইটিং ম্যানেজমেন্ট, লিটার ম্যানেজমেন্ট). No English subtitle line on this list (matches recording). `farm-management/shed-cleaning.html` is the full 7-step detail article, transcribed end-to-end across the recording's scroll.
    - **ভ্যাকসিন তথ্য** (header reads "ভ্যাকসিন সম্পর্কিত"): 6-item list (ভ্যাক্সিনেশন শিডিউল, লাইভ ভ্যাকসিন, কিল্ড ভ্যাকসিন, ভ্যাকসিন দেওয়ার সময় করণীয়, ভ্যাকসিন দেওয়ার সময় সাধারণ ভুলসমূহ, ভ্যাকসিন সফল করার জন্য করণীয়) with English subtitle line under each title. **No detail page tapped in the recording** — all 6 stay as non-linked `.list-card` divs.
    - **রোগ নির্ণয়** (header reads "রোগ নির্ণয় কৌশল"): 6-item list (বাণিজ্যিক ব্রয়লার, বাণিজ্যিক কালার চিকেন, বাণিজ্যিক লেয়ার, ব্রয়লার প্যারেন্ট স্টক, কালার প্যারেন্ট স্টক, লেয়ার প্যারেন্ট স্টক), English subtitle per item. **No detail page tapped** — all non-linked.
    - **রোগ বালাই** (header reads "মুরগির রোগ বালাই"): 6-item list (রাণীক্ষেত, **ব্রংকাইটিস** ← linked, গাম্বোরো রোগ, কক্সিডিওসিস রোগ, সালমোনেলা রোগ, করাইজা রোগ), English subtitle per item. `disease-control/bronchitis.html` is the full detail article (disease description, symptoms, post-mortem signs, prevention/vaccination, economic loss, closing note) transcribed end-to-end from the recording's scroll.
    
    Note: unlike accounting/production-cost, description text here is genuinely truncated with a trailing "…" in the source app itself (long article previews) — kept that ellipsis literally rather than inventing a full sentence for text the recording never fully revealed.

17. **ডাটা ব্যাংক (Data Bank)** — `databank/index.html`, replacing its "not yet built" placeholder. ✅ Functionality-match built (matches structure from `WhatsApp Video 2026-08-03 at 07.08.51.mp4`). **First bottom-nav section other than Home to get real content.** Unlike the accounting/production-cost drill-downs, it *keeps* the app-bar and the bottom nav (ডাটা ব্যাংক active) and has **no `.subpage-header` and no top tabs** — it's a nav-level destination, not a drill-down. Body is a plain scrolling `.list-wrap` of nine `.list-card`s, no section title above them. Zero new CSS. The nine tools: Flock Data Management · Vaccination Schedule Making and Tracking · Flock wise Lab Report input and Analysis · Disease Identification, Treatment and Analysis · Breeder Development Monitor · Feed Formulation for Poultry · Molting Monitoring for Poultry · Investment Calculator and Project Planning · Farm and Hatchery Biosecurity Audit Check. All cards are `<div>`s — none of the nine sub-screens are recorded yet.
    - **Content note: this whole section is in English, not Bengali**, both titles and descriptions, unlike every screen built so far. That's how the original app is; transcribed verbatim including its inconsistent trailing periods (some titles and descriptions end with `.`, others don't) — those are the original's, not typos to clean up.
    - Original uses detailed custom illustrations per card (dashboards, lab benches, vaccine vials); emoji badges stand in per the standing no-real-assets rule, rotating gold/rust/moss down the list.

18. **Flock Data Management → "Today Farm Dashboard"** — `databank/flock-data-management.html`, linked from the first Data Bank card. Static mockup (matches structure from `WhatsApp Video 2026-08-03 at 07.21.25.mp4`). **First dashboard-shaped screen in the app**, so several new components landed:
    - `.dash-head` (title + date), `.stat-grid`/`.stat-tile` (2-column KPI tiles), `.dash-block`/`.dash-meta`, `.summary-head` (heading with a rule under it), `.kv-row` (label + `: value` columns), `.status-line`/`.status-dot`/`.status-list`, `.action-stack`, and a new **`.calc-btn.gold`** variant — gold background with **charcoal** text, not white, matching the original's dark-on-amber and clearing contrast that white-on-gold would fail.
    - Content: 8 tiles (Active Flock 2 · Completed 0 · Current Bird 3022 · Today's Feed 0.0 Kg · Today's Egg 0 · Mortality 0 · Cull/Sale 0 · Today Entry 0), Last Updated block, Farm Summary with 4 cumulative rows, a System Status dot + two "pending" bullets, then 4 full-width action buttons alternating gold/moss (New Flock Create · Daily Flock Data Entry · Flock List, Edit and Delete · FCR / Egg Report). The figures are the recording's, kept as-is.
    - ~~**No back arrow here, deliberately**~~ — **superseded, see the standing back-arrow rule in Conventions.** This originally shipped without a back arrow on the reasoning that the ডাটা ব্যাংক nav item already returns to the Data Bank list. The user asked for it back; a bare `.plain-header` arrow → `index.html` was added.
    - Design-guidance note: these are **bare stat tiles with no plot**, so the charting rules that would normally bind (categorical palette validation, legends, hover/tooltip layer) don't apply — there is no series colour to check. What does apply and was followed: tile values wear ink tokens rather than a brand colour, and the status dot never carries state alone (it always sits beside its "System Status" label and text bullets). Worth re-checking if a later Data Bank screen (FCR / Egg Report, Lab Report Analysis) introduces a real chart.
    - The source recording caught a WhatsApp Business notification popup mid-scroll — ignored, same as the egg-mass recording.

19. **Flock Data Management inner flow — all 4 dashboard buttons** — `databank/flock-create.html`, `flock-daily-entry.html`, `flock-list.html`, `flock-report.html` (matches structure from `WhatsApp Video 2026-08-03 at 07.32.38.mp4`, 197 frames / ~98s — by far the longest recording so far; it walks the whole CRUD loop end to end rather than showing one screen).
    - **First real data-entry flow in the project** (everything before it was a calculator or an article list), so it broke the "static mockup = nothing moves" assumption in a new place. Standing rule still holds — nothing computes — but three *recorded* state swaps are wired: the Flock Type dropdown swapping the production-form block, the "+ Add New Farm" option revealing a New Farm Name field, and the modal dialogs opening. All three are states visible in the recording.
    - All four are **drill-downs that hide the app-bar and bottom nav**, so all four get an added back arrow (`.plain-header`) per the standing deviation. The parent dashboard itself still has none.
    - `flock-create.html` — Farm Name select (Select Farm / abc / hell / **+ Add New Farm**), Flock Name, Shed / House Number, Placement Date (`Example: 2026-04-20`), the computed hint `Age: 0 week 0 day`, Breed / Strain Name, then a **Flock Type** select with 8 options (Broiler / Sonali / Color / Broiler Duck / Layer / Broiler Breeder / Color Breeder / Layer Breeder). Picking **Layer** swaps "Meat Production Form" for "Layer Production Form". Meat fields: Initial Bird Quantity\* · Initial Chick Weight (gm) · Expected Sale Age days · Remarks. Layer fields: Initial Bird Quantity\* · Initial **Chicks** Weight (gm) — note the plural, the original is inconsistent between the two forms — · a **greyed, unlabeled field pre-filled `21`** (no placeholder text in the original; rendered as a disabled input showing 21) · Remarks. Only Broiler→Meat and Layer→Layer are recorded, so the swap keys on Layer alone; the other six types fall through to Meat and may be wrong. Original colours the two section titles teal-green and **purple** — purple isn't in our token set, so Meat is moss and Layer is rust.
    - **Farm/flock names replaced with meaningful ones, per user request.** The recording's actual data was test-keyboard-mashing (`abc`, `hell`, `Ff`, `Hhg`, `Janina`) — realistic on a QA device, unreadable as a demo mockup. Swapped throughout `flock-list.html`, `flock-create.html`'s farm select, the two picker dialogs on `flock-data-management.html`, and the daily-entry/report page titles for: **Green Valley Farm** (Broiler, was `abc`) with flocks **Broiler-221** (was `Ff`, Shed 221) and **Broiler-H** (was `Janina`, Shed H); **Sunrise Poultry Farm** (Layer, was `hell`) with flock **Layer-JG** (was `Hhg`, Shed JG — also fixed the shed casing, `Jg`→`JG`, to match the flock-name suffix). All other figures (bird counts, feed, mortality, dates) are untouched — only the free-text names changed.
    - `flock-daily-entry.html` — title carries live context: `Layer-JG - Daily Entry - Rearing Mode (0 wk)`. Date (pre-filled) · Feed Kg · `Opening Bird: 1` (charcoal) · Mortality · Culling / Sale · `Closing Bird: 1` (moss, recomputes in the original) · Remarks · SAVE DAILY ENTRY.
    - `flock-list.html` — `Flock List` with **Active / Completed** pill tabs (reuses the existing `.tabs`/`.tab`), records grouped under `Farm: <name>` headings, each a card with name, `Type: … | Start Bird: …`, Start Date, Age, then Shed / Current Bird / Feed / Mortality | Culling/Sale / Avg Weight / Live Weight / Current FCR, and an EDIT (moss) + DELETE (rust) pair. DELETE opens the recorded confirm dialog (`Delete Flock` / "এই flock এবং এর সব daily entry delete হবে। আপনি নিশ্চিত?"), which cancels either way.
    - `flock-report.html` — `Broiler-221 - Report`, From Date / To Date side by side, FILTER REPORT, then two **fixed-width** blocks: a Type/Current Age/Starting Bird/Current Bird/Body Weight/FCR summary and a Date/Feed/Mort/Cull table with dashed rules and a TOTAL row. The original genuinely renders these as preformatted monospace text (not a styled table), and the columns only line up in a monospace face — hence `.report-card pre` rather than a real `<table>`. A floating green `রিপোর্ট শেয়ার` FAB sits bottom-right; in the original it exports a PDF (`Flock_Report_<timestamp>.pdf` via the Android share sheet) — inert here.
    - Both "select a flock" dialogs live on the **dashboard**, not on the target pages: Daily Flock Data Entry and FCR / Egg Report each open a picker listing `farm | Shed: x | flock (Type)` before navigating. New Flock Create and Flock List navigate straight through. All picker options lead to the same single mockup page.
    - New CSS: `.form-label`, `.form-hint` (+`.ink`), `.form-section-title` (+`.moss`/`.rust`), `.field-row`, `.calc-input:disabled`, `.modal-overlay`/`.modal-card`/`.modal-title`/`.modal-text`/`.modal-option`/`.modal-actions`/`.modal-btn` (+`.danger`), `.group-label`, `.record-card`/`.record-key`/`.record-sub`/`.record-age`/`.record-facts`/`.record-actions`, `.report-card`, `.share-fab`. Also `position:relative` added to `.screen` (anchors overlays and the FAB) and `text-decoration:none` to `.calc-btn` (it's now used on `<a>` as well as `<button>`).
    - The dashboard's own figures were **not** updated from this recording even though they differ (Current Bird 2938 vs 3022, etc.) — same screen, later in the session with more data entered. Left as built.

20. **Vaccination Schedule Making and Tracking** — `databank/vaccination-schedule.html`, `vaccine-dashboard.html`, `vaccine-flock-new.html`, `vaccine-flock-edit.html`, `vaccine-breed-schedule.html`, linked from the second Data Bank card (matches structure from `WhatsApp Video 2026-08-03 at 08.01.03.mp4`, 130 frames / ~65s). The deepest nesting yet — a page that opens a modal that itself opens a second modal.
    - `vaccination-schedule.html` — the nav-level landing screen, same shape as Flock Data Management: keeps the app-bar and bottom nav (ডাটা ব্যাংক active), plus a bare `.plain-header` back arrow → `index.html` (added later, see the standing back-arrow rule in Conventions). Intro paragraph, gold `VACCINATION SCHEDULER MODULE OPEN করুন` button, then an embedded **"আজকের Vaccine Calendar"** — a real always-on calendar grid (new `.calendar-card`/`.calendar-grid`/`.calendar-nav`), not a popup date-picker — with 3 August 2026 circled as today, and a due-today card below (new `.due-card`, amber) reading `Flock: GGGG (Gumboro/IBD Disease) · Day 10 · Due Date: 2026-08-03 · Pending`. **Colour substitution**: the recording's "today" circle is Android's system teal accent (matches the native date-picker dialog seen earlier in `flock-create.html`), not an app brand colour — mapped to `--moss` instead, same precedent as temp-converter's CLEAR button.
    - `vaccine-dashboard.html` — "Poultry Vaccine Dashboard", reached by the OPEN button. **Reuses `.stat-grid`/`.stat-tile` verbatim** from Flock Data Management's dashboard for its 4 tiles (Total Flocks / Today Due / Completed / Missed) — zero new CSS for that part. Two full-width buttons (moss `BREED WISE SCHEDULE বানান`, gold `NEW FLOCK যোগ করুন`), then a Flock List section with a search input and one flock card. The card uses a new alert-tinted variant (`.record-card.alert`, rust-soft background) since this card is pink in the recording where Flock Data Management's own cards are white — status data (Breed/Birds/Age/Due Today/Missed/Completed%) plus a bold `Status: Critical` line (new `.record-status`) and an italic `Long press to delete` hint (new `.record-hint`), then OPEN (gold) + EDIT (moss) buttons.
    - This screen and the two flock-form pages below hide the app-bar/bottom nav in the recording but **show no header text of their own either** — the capture cuts straight from status bar to page content with nothing in between, unlike every other drill-down so far. Treated as a capture/original-app gap rather than a real "no header" design: added a `.plain-header` + back arrow to each per the standing deviation (hidden nav → needs a way back), same as always, just noting here that the title text itself (`Poultry Vaccine Dashboard`, `New Flock`, `Edit Flock`) is our own label choice, not read off a visible header in the recording.
    - `vaccine-flock-new.html` / `vaccine-flock-edit.html` — Flock Name, breed `<select>` (Broiler/Color/Sonali/Broiler PS/Layer PS/Color PS/Layer — a **different 7-item list** than Flock Data Management's 8-item Flock Type select, note the missing "Broiler Duck" and the "PS" suffix style), a disabled date field + gold `START DATE SELECT করুন` button (the recording shows the same native Android date-picker dialog already documented for `flock-create.html`, not rebuilt again here), Total Birds, Shed Name, Remarks, then a save/update button. Edit is the same layout prefilled with `GGGG` / Broiler / 2026-07-25 / 3000 / `Ff` / `tika` — note **`Ff` here is a shed name on this screen's edit form**, not the same entity as `Broiler-221` (formerly `Ff`) in the Flock Data Management flow; these are two unrelated demo records that both happened to get typed as "Ff" during testing. Left as-is since renaming it here could wrongly imply a cross-flow link that doesn't exist in the recording.
    - `vaccine-breed-schedule.html` — reached by the dashboard's `BREED WISE SCHEDULE বানান` button. Breed `<select>` at top (Common Demo default, plus the same 7 breeds) whose `onchange` shows a toast pill (new `.toast`, auto-hides after ~2.6s) reading `<breed> breed এ 18 টি demo schedule পাওয়া গেছে` — a state swap, not a real reload; the day-list underneath never actually changes with it, matching the recording (Broiler and Color showed identical content). Below that, two buttons open two independent modal chains:
      - Orange `নতুন রোগ যোগ করুন` → **"নতুন রোগ ও ভ্যাকসিন যোগ করুন"** modal (add a whole new disease+vaccine pair): রোগের নাম, Description, ভ্যাকসিনের নাম, Route, Dose, CANCEL/SAVE.
      - Green `সিডিউল বানান` → **"কোন Breed-এর জন্য Schedule যোগ করবেন?"** modal (new `.modal-radio-list`/`.modal-radio`, a radio list of the 7 breeds) → NEXT closes it and chains straight into a second modal, **"নতুন সিডিউল"**: Disease `<select>` (16 options, alphabetical, read off two disease-dropdown-open frames), Vaccine `<select>` (17 options, one flat pool — the recording never showed it filtered by disease, so it isn't here either), Day Number, Dose (placeholder literally reads **"As per label"** — that's the app's real placeholder text, not typed junk), Route (placeholder literally reads **"WW"**, likewise real), Remarks, CANCEL/SAVE.
      - Each of the 4 demo `.sched-card`s (`Day 1 - Marek's disease` / `Day 5 - Newcastle Disease` / `Day 5 - IB Disease` / `Day 10 - Gumboro/IBD Disease`, each showing Vaccine/Route/Dose/Remarks) has its own gold EDIT button opening a shared **"সিডিউল এডিট"** modal, prefilled via a small JS helper (`openEditSchedule(...)`) from that card's own data — same field order as "New Schedule" (Disease, Vaccine, Day Number, Dose, Route, Remarks) with an UPDATE button instead of SAVE.
      - **Only 4 of the app's claimed ~18 demo schedule rows ever appear on screen** — the recording never scrolls further, so the other 14 aren't guessed at. New small-button modifier `.calc-btn.sm` (inline-width, for EDIT pills inside cards rather than the default full-width button) landed here, reusable elsewhere.
    - `vaccine-dashboard.html`'s OPEN button (per-flock vaccine tracker, presumably the Day-by-day due/missed view scoped to one flock) was never tapped in the recording — left inert, same convention as other unrecorded destinations.
    - New CSS in full: `.section-title-center`, `.calendar-card`/`.calendar-nav`/`.calendar-grid`/`.calendar-date`, `.due-card`, `.calc-btn.sm`, `.record-card.alert`, `.record-status`, `.record-hint`, `.sched-card`, `.modal-card.scroll`, `.modal-radio-list`/`.modal-radio`, `.toast`.

21. **Molting Monitoring for Poultry** — `databank/molting-monitoring.html` (dashboard), `databank/molting-guide.html` (protocol article), `databank/molting-add.html` (add-flock form + decision tool), linked from the Data Bank list's 7th item (`databank/index.html`, which — along with `flock-create.html`/`flock-daily-entry.html`/`flock-data-management.html`/`flock-list.html`/`flock-report.html` — was added directly to the repo outside this conversation; not reverted, just built on top of). Matches structure from `WhatsApp Video 2026-08-03 at 7.52.49 AM.mp4`. Notable findings:
    - **4th header pattern**: none of the 3 molting screens use `.subpage-header`/`.plain-header`/`.inset-header` — instead a bare bold-black left-aligned title with no colored bar at all (new `.page-head`, 21px Baloo Da 2 700 title + moss back arrow, no app-bar/bottom-nav on any of the 3 screens, matching flock-create.html/flock-daily-entry.html's precedent of dropping bottom-nav on drill-down forms).
    - **`molting-monitoring.html`** (dashboard): two full-width **moss** (not gold) buttons stacked — ADD NEW MOLTING FLOCK, MOLTING PROTOCOL GUIDE — reusing `.calc-btn.moss`. Below, the existing `.stat-grid`/`.stat-tile` component reused for a 4-tile KPI row (Active/Danger/Recovery/Pending Entry), but this is the **first screen needing 4 distinct status colors** on stat values — added `.stat-value.moss/.rust/.gold/.info` modifiers, and since the recording's "Recovery" tile is genuinely blue and no blue existed anywhere in the palette, added one new token `--info-blue:#3B6E91` (status-only use, not a general palette addition). Empty state below: "Active Molting Flocks" heading + gray "No active molting flock" text (new minimal `.empty-state` class) — all 4 tiles default to 0 since no flock has been added yet, matching the recording exactly (not synthesized).
    - **`molting-guide.html`**: a single long article reusing the `.article-body`/`.article-h`/`.article-divider` components built for the खামার ব্যবস্থাপনা/রোগ বালাই sections — proof that template is generic enough for a second, unrelated content domain. Content is numbered sections (1–8: pre-molting checklist, exclusion list, day-by-day feed/light/water phases for both "Moderate Controlled" and "Aggressive" strategies, target-check phase, recovery phase, second-cycle monitoring, app decision-rule legend), transcribed in full off the recording's scroll, English section headers with mixed Bengali/English ✔/❌ bullet content exactly as recorded.
    - **`molting-add.html`**: the most complex tool screen yet. `.calc-card` with Flock Name/Bird Number/Age/Current Production %/Start Body Weight text inputs (all `disabled`, empty placeholders per standing convention) + Flock Type `<select>` (Layer/Breeder, both confirmed in the recording's opened dropdown). A moss "SELECT MOLTING START DATE" button with a **new `.field-tag` class** underneath showing "Start Date: 2026-08-01" — the actual date the recording landed on after tapping (shown filled per the "every element visible" convention, unlike the still-empty text inputs above it, since this is closer to a result readout than a raw input). "Market and Prediction Data" block (new `.form-block-title` heading, reused for "Pre-Molting Checklist" too) with 6 more disabled/empty inputs, then 4 `.field-tag` + `<select>` pairs — **Mortality Trend (Low/Medium/High), Body Condition (Good/Fair/Poor), Market Demand (Low/Medium/High), Molting Strategy (Auto Recommended/Moderate Controlled/Aggressive)**. Only one option per dropdown was actually visible in the recording (their menus were never opened) — the other options are confident completions of an obvious 3-tier scale (Low/Medium/High, Good/Fair/Poor) rather than invented app-specific data, and "Moderate Controlled"/"Aggressive" for the strategy dropdown are directly evidenced by the protocol guide's own section headers, not guessed. Flagging this explicitly since it's a looser evidence bar than the "only include confirmed options" rule used for breed dropdowns earlier — reasonable here because these are generic scale labels, not domain facts. Then an 8-item **checklist** (new `.checklist-row`, real enabled `<input type="checkbox">`s, unchecked by default — checkbox toggling is UI-state-only like the select dropdowns, not a calculation) matching the recording's Pre-Molting Checklist items (a shorter subset of the guide's step-1 ensure-list). Final moss "CHECK DECISION" button, and below it the **real captured decision output** from the recording (typed-in demo values: Layer, 2000 birds, age 5wk, feed intake 100g, feed price 90, egg price 12, post-molt production 10%, cycle 20wk) — a red "❌ NOT RECOMMENDED" verdict with Health/Age/Market scores, recommended strategy, income/cost/profit figures, and a Bengali advice line, rendered as plain colored text (new `.decision-result`/`.decision-flag`) rather than a bordered card, matching how the recording shows it (no box, just red text under the button).
    - **Upgraded design pass, 2026-08-03** (user asked to improve both `molting-monitoring.html` and `molting-add.html`, same session as the CAPA Tracker / Bio-Security Audit redesigns): same underlying data throughout, restyled only.
      - `molting-monitoring.html`: the two stacked buttons now read gold (primary "+ Add New Molting Flock") vs. new `.calc-btn.outline` (secondary "📘 Molting Protocol Guide") instead of two identical moss buttons — makes the create action stand out. "No active molting flock" plain text became a new `.empty-card` (feather emoji badge-circle + two-line message), matching the icon-badge treatment used everywhere else instead of a bare gray line.
      - `molting-add.html`: the flat single-block form got real section headers (new `.form-section-title.moss` + emoji, reusing the existing moss/rust color modifier) — Basic Flock Info 🐔 / Market and Prediction Data 📈 / Risk and Strategy Assessment 🎯 / Pre-Molting Checklist ✅. Every input now has a bold `.form-label` above it (was placeholder-only text before) — same fields, clearer to scan. The 4 `.field-tag`+`<select>` decision inputs (Mortality Trend/Body Condition/Market Demand/Molting Strategy) became **chip-style single-select pickers** (new `.chip-select-row`/`.chip-radio`/`.sr-only` — visually-hidden radio + pill label, pure CSS `:checked` state, no JS) instead of boxed dropdowns, matching the "chip-style pill tags instead of plain text" design-system move. CHECK DECISION is now gold (was moss) to read as the page's primary action. The decision output — previously a single wall of rust-colored text — became a scannable stack: a `.decision-banner` alert card for the ❌/✅ verdict, the 3 scores as `.stat-grid.cols-3` tiles (color-graded: ≥70% moss, 40–69% gold, <40% rust — Age Score 40% lands on the gold boundary), a `.decision-strategy` pill for the recommended strategy, the strategy description as plain `.decision-note` text, financial figures reusing the existing `.info-block`/`.info-row` component (Net Profit tinted rust since it's negative), and the Bengali advice line moved into the existing `.notice` tip-box component. New CSS in full: `.calc-btn.outline`, `.empty-card`, `.sr-only`/`.chip-select-row`/`.chip-radio`, `.decision-card`/`.decision-banner` (+`.no`/`.yes`)/`.decision-strategy`/`.decision-note` (the old `.decision-result`/`.decision-flag` rules were removed — confirmed unused elsewhere first).

22. **Farm and Hatchery Biosecurity Audit Check** — `databank/biosecurity-audit.html` (landing), `biosecurity-executive-summary.html`, `biosecurity-audit-history.html`, `biosecurity-farm-audit.html`, `biosecurity-hatchery-audit.html`, `biosecurity-previous-reports.html`, `biosecurity-pdf-reports.html`, `biosecurity-pdf-report.html`, linked from the Data Bank list's 9th (last) card. Matches structure from an **image-folder source, not a video** — `C:\Users\Mahmud\Downloads\ezgif-87dc679cdccd2aed-jpg\` (240 numbered JPG frames, already pre-extracted — no ffmpeg step needed, per the 2026-08-03 workflow update). This is the largest single feature so far, and the one place scope had to be deliberately capped:
    - **`biosecurity-audit.html`**: bare `.page-head` (no h1, just back arrow — the recording's own title lives inside the body as a big centered hero, not a header bar), new `.audit-hero` (24px Baloo Da 2 800 centered title + gray subtitle) and `.audit-summary-card` (mint `--moss-soft` card, "Executive Dashboard": Farm Audits 0 | Hatchery Audits 1, Average Score 83%, High/Medium/Low Risk, Open/Completed CAPA — real captured figures). Below it, a new **accordion menu** (`.audit-menu`/`.accordion-bar`/`.accordion-panel`/`.accordion-link`, toggled by a small inline `toggleAccordion()` — exclusive open, opening one closes the other, matching the recording exactly) with two sections:
      - **Management Dashboard** (dark moss-dark bar) expands to 6 **solid-color pill** sub-buttons: Executive Summary (indigo) → linked, Bio-Security Audit History (teal) → linked, Bio-Security Repeated Findings (red), Audit Comparison (teal), Risk Trend Graph (indigo), Performance Ranking (teal) — only the first two were actually tapped into in the source images, the other 4 are inert per the standing "only wire what's shown" rule.
      - **Reports & Compliance** (teal bar) expands to 4 **flat tinted rows** (new `.accordion-link.soft` modifier) — Previous Reports (plain) → linked, Critical Alerts (rust-soft/rust text), CAPA Tracker (plain), PDF Reports (indigo-soft/indigo text). This panel's sub-items are visually a *different* treatment than Management Dashboard's (flat tint vs solid pill) — read directly off the source images, not assumed shared.
      - Two plain white full-width rows in between, **Farm Data Entry** and **Hatchery Data Entry**, both linked (new `.audit-plain-row`).
      - **New colour**: the source images' navy/indigo bars (Management Dashboard, Executive Summary, Risk Trend Graph, PDF Reports text) don't match any existing token — reused `--info-blue` (already added for Molting's Recovery stat tile) rather than inventing a second blue. A new pale tint `--indigo-soft:#E7E8F3` was added for Executive Summary's report card since nothing in the palette was light enough. The red items reuse `--rust`/`--rust-soft` as-is.
      - **Upgraded design pass, 2026-08-03** (same session as CAPA Tracker's redesign below — user disliked the original solid-color-block look on both): `biosecurity-audit.html` was restyled from scratch. Executive Dashboard's plain text paragraph became a `.stat-grid.cols-3` (6 tiles: Farm/Hatchery Audits, Avg Score, High/Medium/Low Risk) + a small centered Open/Completed CAPA note. Farm/Hatchery Data Entry became a 2-up `.entry-grid` of icon cards (new component) instead of plain bordered rows. The accordion itself was rebuilt as `.menu-card`/`.menu-card-head`/`.menu-panel`/`.menu-row` — each section is now a single white shadowed card containing icon-badge rows (`.menu-row-icon` + `.indigo`/`.red`/`.moss`) with a trailing `›` chevron, replacing the old solid-color pill buttons (Management Dashboard) and flat tinted rows (Reports & Compliance) with one consistent, softer treatment. `toggleAccordion()` renamed `toggleMenu()`, same exclusive-open behavior. Old classes (`.accordion-bar`/`.accordion-panel`/`.accordion-link`/`.audit-plain-row`) were fully replaced, not kept as aliases — confirmed unused elsewhere first. The bottom "prototype/static mockup" caption line was also dropped per user request (no longer used on this page or CAPA Tracker; still present on other Bio-Security Audit sub-pages unless asked to remove there too).
    - **`biosecurity-executive-summary.html`**: `.audit-hero h1` in indigo, then a new `.audit-report-card` (pale `--indigo-soft` box, plain-text report style) with the real captured figures (Total Audits 1, Farm Audits 0, Hatchery Audits 1, Grade/Risk/CAPA breakdowns, Best/Worst Performing both "HATCHERY - Savar Hatchery (83%)", Most Common Finding "-"), then an indigo "Export Executive Summary PDF" button.
    - **`biosecurity-audit-history.html`**: a **working** Farm/Hatchery radio toggle (reuses `showFieldGroup()`/`.field-group`, same pattern as ROI/FCR) swapping between an empty "Select Farm" state (Farm Audits is 0, so nothing to show) and a "Select Hatchery" `<select>` defaulting to the one real record, `Savar Hatchery` — picking it (or, by default, the page loading with it pre-picked) reveals the real captured result: `Savar Hatchery / Total Audit: 1 | Avg Score: 83% / Total Critical Findings: 0` plus a `.record-card` (Date: 03-08-2026 | Type: HATCHERY, Answered: 60/60, Critical Findings: 0). Zero new CSS — reuses `.radio-row`, `.field-group`, `.record-card`.
    - **`biosecurity-farm-audit.html`** / **`biosecurity-hatchery-audit.html`**: the two 60-question checklist forms (confirmed by the Audit History card's "Answered: 60/60"). **Scope was deliberately capped** — transcribing all 60 questions × options for both forms wasn't practical from the available frames, so each form ships a **representative real subset**, explicitly labelled in its caption (Farm: 20 of 60, categories A–J; Hatchery: 17 of 60, categories A, C, D, F, J, K) — every question, category label, and option shown **was** read directly off a source image (nothing invented), just not the full 60. New components: `.uline-field` (underline-only text/select field, distinct from the boxed `.calc-input` — matches the recording's minimalist header-field style used for Date/Select Farm/Name/Manager/Location/Inspection Team) and `.audit-card`/`.audit-cat`/`.audit-q`/`.audit-options`/`.audit-opt` (category label + numbered question + wrapping horizontal radio row + Remarks field, repeated per question). Radios are left enabled (harmless native toggle, same precedent as Molting's checklist checkboxes) while text inputs stay `disabled`. A few option labels that were cut off at the phone's screen edge in every sampled frame were completed only where the exact same phrase appears **fully spelled out elsewhere** in the same question set (e.g. the recurring `Excellent/Good/Fair/Not Good` and `Working properly/Working partially/Not installed` patterns) — noted here since it's a slightly looser evidence bar than usual, same category as Molting's generic-scale dropdown completions.
    - **Two rounds of user-supplied real-app corrections, 2026-08-03** (the app itself, not the source images — screenshots came in after the initial build):
      1. First correction: the report-detail content (heading `BIO-SECURITY HATCHERY AUDIT REPORT` + `====` divider + `Audit Date:` line, plain-white monospace card) had been linked straight off "PDF Reports", but the real app reaches it via **Previous Reports → report list → tap a card**. Added `biosecurity-previous-reports.html` (list: "Previous Bio-Security Reports" hero + `.group-label` "🏭 Hatchery Reports (1)" + one `.record-card`, no Farm Reports section since Farm Audits is 0) as what "Previous Reports" now links to, and fixed the detail page's missing heading/divider/Audit-Date line.
      2. Second correction: a further screenshot showed "PDF Reports" is in fact its **own distinct list screen** (not the same as Previous Reports, and not just an inert button) — indigo "PDF Reports" hero, "Total Audit Reports: 1" line, and a `.record-card` with a *different* fact set (Type/Date/Score/Rating/Risk/Critical Findings, not Date/Type/Answered/Critical Findings) plus a **View PDF** (moss-dark) / **Share PDF** (indigo) button pair — reusing `.record-actions`. Added `biosecurity-pdf-reports.html` for this, linked it from "PDF Reports", and pointed its **View PDF** button at the same `biosecurity-pdf-report.html` detail page (the two list screens converge on one real detail page — reasonable since both list a single audit and neither showed a second, different detail view). Previous Reports' own card was reverted to non-interactive (no confirmed tap-through for it specifically) now that View PDF is the confirmed path to the detail. `biosecurity-pdf-report.html`'s back arrow now points at `biosecurity-pdf-reports.html`.
      3. User also asked for the placeholder entity name **`test`** to be replaced with something meaningful — renamed to **`Savar Hatchery`** everywhere it appeared (Audit History's select/result/record-card, Executive Summary's Best/Worst Performing lines, the Hatchery Data Entry form's `<select>`, Previous Reports' and PDF Reports' cards, and the report detail's title/`Hatchery Name:` line). `Manager/In-charge: faruk` and `Location: dhaka` were left as recorded — only the entity name itself was flagged as needing a real-sounding value.
    - New CSS in full: `.audit-hero`, `.audit-summary-card`, `.audit-menu`/`.accordion-bar`/`.accordion-panel`/`.accordion-link` (+`.indigo`/`.red`/`.soft`), `.audit-plain-row`, `.audit-report-card`, `.uline-field` (+`.select`), `.audit-card`/`.audit-cat`/`.audit-q`/`.audit-options`/`.audit-opt`. New tokens: `--indigo-soft`. (`biosecurity-pdf-report.html` and `biosecurity-pdf-reports.html` reuse the pre-existing `.report-card`/`.record-card`/`.record-actions` components, not new classes.)

23. **CAPA Tracker (Corrective & Preventive Action)** — `databank/capa-tracker.html`, linked from the Biosecurity Audit accordion's Reports & Compliance panel (was inert, now built). Matches structure from `WhatsApp Video 2026-08-03 at 9.06.41 AM.mp4`. Reuses `.page-head`/`.audit-hero`/`.radio-row`/`.empty-state` wholesale.
    - **Upgraded design pass** (user disliked the first functionality-match version — solid-color banner headers with white text, felt heavy/utilitarian): replaced the summary block with the existing `.stat-grid`/`.stat-tile` component (6 tiles: Pending/In Progress/Completed + High/Medium/Low priority, reusing the already-defined `.stat-value.rust/gold/moss/info` colors — zero new CSS for that part) instead of a plain text paragraph. Each finding card now leads with a `.badge-circle` emoji icon (color-tinted via the existing `.badge-rust/.badge-gold` per its priority, same signature move used everywhere else in the app) + title/subtitle, followed by two small pill `.tag-pill` chips (priority + status, new component, colors mapped to existing tokens — status uses a neutral/info-blue/moss traffic-light scheme distinct from priority's rust/gold/moss so the two chip rows don't read as the same signal), then a labelled Corrective/Preventive block (new `.capa-fix`/`.capa-fix-row`/`.capa-fix-label`) above the Update Status button. Same underlying data as the first pass, just restyled.
    - **Working Farm/Hatchery type filter + All/Pending/In Progress/Completed status filter**, both real JS (`filterCapa()`) toggling which already-authored cards are visible via `data-type`/`data-status` attributes — same "swap which recorded state is shown" pattern as the audit-history Farm/Hatchery toggle, not a calculation. Default on load (confirmed from the recording's opening frame): Farm + All.
    - The Farm summary card shows the real captured Executive-Dashboard-style stat line (Pending 11 | Progress 1 | Completed 0, High 9 | Medium 3 | Low 0). Hatchery has no confirmed CAPA items in the recording, so its summary/empty state defaults to all-zero rather than inventing entries.
    - **Scope capped, same as the 60-question audit forms**: the recording scrolled through 8 of the Farm list's 12 real items (7 Pending + the 1 In Progress) before the "Update Status" tap covered the rest with a modal that never re-revealed them — shipped exactly those 8, captioned as a representative sample, rather than guessing the remaining 4. One card ("Dead bird disposal is improper") was left out entirely even though its header/priority/corrective line were readable, because its Preventive line was cut off at the bottom of frame in every sampled screenshot and never became readable — dropped rather than inventing the missing half of a real card.
    - **Change Status modal**: the recording shows the modal open (title "Change Status" only, white card over a dimmed background) but its interior content never rendered in any captured frame at any sampled timestamp — genuinely blank in every frame. Built the modal with a status `<select>` using the three status values already confirmed elsewhere on this exact screen (Pending/In Progress/Completed, from the status filter and the cards' own `Status:` lines) plus Cancel/Save, rather than either inventing unconfirmed fields or shipping a title-only dialog — flagged here since, like the molting decision dropdowns, this reuses only vocabulary already confirmed on the same screen.

**Update (2026-08-03, later same day):** user asked to remove the bottom "ETI-App prototype · ..." caption line **from every page** — it had been added to the bottom of every screen since early in the build (scope notes, static-mockup disclaimers, navigation hints). Stripped the `<div class="caption">...</div>` line from all 50 files that had it (every screen built so far) and deleted the now-unused `.caption` CSS rule. **This is now the standing convention — new screens should not add a bottom caption div going forward.** Any scope/evidence disclosures that used to live in a caption (e.g. "showing 20 of 60 questions") are still documented here in CONTEXT.md, just no longer surfaced in the UI itself.

24. **Flock wise Lab Report input and Analysis** — `databank/lab-report.html` (flock list), `lab-report-detail.html` (flock detail + report actions), `lab-report-view.html` (report table), `lab-report-graph.html` (titer graph), linked from the Data Bank list's 3rd item. Matches structure from a **pre-extracted screenshot folder** (`WhatsApp_Video_2026-08-03_at_08_24_05_frames/`, 49 PNGs) rather than a video this time — read directly, no ffmpeg step needed.
    - **Mixed shell behaviour within one feature**: the list and detail pages keep the full app-bar + bottom nav (ডাটা ব্যাংক active), but `lab-report-view.html` and `lab-report-graph.html` strip the shell entirely. Confirmed per-screen by checking whether the hamburger/app-title row is actually present in that screen's own frames, rather than assuming one behaviour for the whole feature — the earlier Vaccination Schedule build had every sub-page stripped, this one doesn't. **All four carry a back arrow** regardless (see the standing rule in Conventions); `lab-report-detail.html`'s was the load-bearing one, since the bottom nav there points at `index.html`, not at its actual parent `lab-report.html`.
    - `lab-report.html` — new `.search-bar` (icon-in-input, pill-shaped) for "Flock Search here...", gold `ADD FLOCK` button opening an **"Add New Flock"** modal (Farm `<select>` with `+ New Farm` → reveals New Farm Name, plus Flock Name/Start Date/Shed No/Age (Weeks)/Bird Type), a plain-text group heading (new `.group-label.neutral` — charcoal, not the existing moss `.group-label` used elsewhere, since this app's farm-group heading isn't colored here) reading the farm name `adne`, then two flock cards with a colour-coded status line (new `.status-good`/`.status-moderate`, plain bold text — green/orange, no pill background). **Only the `hhvv` card links through** (`.record-card` as `<a>`); the `hh` card was never opened in the recording, so it stays a plain (unlinked) card — same "don't guess a destination" rule as the production-cost list.
    - `lab-report-detail.html` — plain heading `Flock: hhvv` + stacked facts (reusing `.record-facts`, no card wrapper — matches the recording's plain-text-on-background layout) and a filled **`.status-pill`** badge (new — pale background + bold dark text, `.moderate`=gold-soft used here; `.good` variant added for symmetry though never shown in this recording). Below it, 5 action buttons, three of them **new colours**: moss `Add New Report` (opens a report-type picker), a new **`.calc-btn.info`** (reuses the existing `--info-blue` token from the molting dashboard) for `View Report`, a **new side-by-side pair** via the existing `.calc-btn-row` — `.calc-btn.purple` (new `--action-purple` token) for `Export PDF` and `.calc-btn.teal` (new `--action-teal` token) for `Share PDF` — and gold for `View Graph`. Purple and teal are genuinely new tokens (not substitutions) because this is a considered two-button pair that needs to stay visually distinguishable, unlike earlier one-off stray colours (chick-quality's blue, temp-converter's grey) that could safely fold into an existing token. Export/Share PDF were never tapped through in the recording (no export/share destination exists to build) — buttons are inert, consistent with convention.
    - **"Select Report Type"** modal (`.modal-option` list: Titer Report / Rapid Test / Drug Sensitivity / Postmortem Report) — only **Titer Report** chains into a second modal, **"Add Titer Report"** (Test `<select>` with ND HI/H9N2 HI/H5N1 HI/IBD ELISA/`+ New Test` → reveals New Test Name, a pre-filled date, Mean Titer, CV %, CANCEL/SAVE); the other three report types were never opened, so they just close the picker with no chained form. **Deliberately not replicated**: the recording briefly shows a red "New Test Name required" validation tooltip when Save is tapped with the field empty — skipped as real form-validation behaviour, which this project doesn't model anywhere else (every other tool screen's Save/Calculate button is inert, not validating).
    - `lab-report-view.html` — new `.test-chip` (pale-blue pill, reusing `--info-blue` + a new `--info-blue-soft` background) showing the single recorded test type `ND HI`, then a new `.data-table` (real `<table>`, header row on `--bg-sand`) with Date/Age/Mean columns and one row (`03-08-2026 · 0 wk 0 d · 25.00`). Only one test type ever appears since only one was recorded — no second tab was invented.
    - `lab-report-graph.html` — **first real chart in the project** (Titer Graph: hhvv), so the `dataviz` skill's form/mark guidance applies even though it's about as minimal as a chart gets: a single data point per series, so no legend and no categorical palette to validate (one series needs neither). Two stacked `.chart-card`s (Mean Titer, CV %) — new component, hand-built inline SVG (thin `--border-soft` gridlines, a `--text-charcoal` axis, the single point direct-labeled above the mark rather than needing a hover layer). **Colour note**: the recording's data-point is a plain red dot — mapped to `--rust` rather than adding a chart-specific token, since rust already reads as "a marked value" elsewhere in the app (Status: Critical, due-card Pending) and is close to the source hue, unlike the Export/Share PDF case above where two *different* new colours were genuinely needed side by side.
    - New CSS in full: `.search-bar`, `.group-label.neutral`, `.status-good`/`.status-moderate`, `.status-pill` (+`.moderate`/`.good`), `.calc-btn.info`/`.purple`/`.teal`, `--info-blue-soft`, `--action-purple`, `--action-teal`, `.data-table`, `.test-chip`, `.chart-card` (+ svg axis/grid/point/label classes).

25. **Disease Identification, Treatment and Analysis** — `databank/disease-id.html` (Step 1) → `disease-id-step2.html` … `disease-id-step5.html` → `disease-id-result.html`, linked from the Data Bank list's 4th item. Matches structure from the pre-extracted folder `WhatsApp_Video_2026-08-03_at_08_50_04_frames/` (29 PNGs).
    - **First multi-step wizard in the project.** Built as **six separate HTML files** rather than one page with JS step-swapping: Back/Next become plain `<a href>` links with no state machine, every step is directly openable for review, and it matches the multi-page-static-site convention. (The `.field-group` JS-toggle pattern from `roi-fcr.html` is for swapping *within* one screen; a 5-step flow is five screens.)
    - The whole flow **strips the app-bar and bottom nav** — just a centered bold title + gray subtitle (new `.wizard-head`). Per the standing back-arrow rule, **only Step 1 gets a `.plain-header` arrow** (→ `index.html`); Steps 2–5 and the result already carry a visible **Back** button that walks the chain, so a second affordance would be noise, and the rule's purpose (never a dead end) is already met.
    - Steps: **1** Bird Type (4 options: Broiler / Meat Type · Layer · Breeder · Duck) · **2** Age Group (8: 0-7 days · 8-21 days · 22-42 days · 43-90 days · Grower · Pre-lay · Production · Breeder Production) · **3** সিম্পটম (**28** Bengali symptoms) · **4** Postmortem ছবি আপলোড · **5** Lesion (**28** English lesions) · then the result. All checkbox lists are a new `.check-card`/`.check-grid`/`.check-item` two-column grid, filled **row-major** (left→right), matching the original. Checkboxes are real and tickable (UI state, not computation — same basis as `molting-add.html`'s checklist), but **nothing is carried between steps**, which the captions say outright.
    - Step 1's footer is a lone full-width **Next**; Steps 2–4 are Back+Next; Step 5 and the result are Back+**Analyze**. The footer is a new `.wizard-nav` using **`position:sticky; bottom:0`** — that single rule reproduces both observed behaviours: it sits in normal flow on the short steps (1, 2, 4) and pins to the viewport bottom on the long scrolling ones (3, 5, result). **One deliberate deviation**: the original's floating buttons let list content scroll visibly behind them (a checkbox row is half-covered mid-scroll); ours gets a solid `--bg-sand` band so the bar reads as a footer rather than as a rendering glitch.
    - Step 4 — a blank white image-preview box (new `.upload-preview`), moss `Postmortem ছবি আপলোড করুন` button, `No image selected` note (new `.upload-note`), and a free-text field for `অতিরিক্ত তথ্য লিখুন: বয়স, mortality %, farm history`. The upload button is inert; no file picker is wired up (consistent with Export/Share PDF on the Lab Report screen).
    - `disease-id-result.html` — the app renders this as **one long formatted text document**, not a set of cards: selection echo (Bird Type / Age Group / Selected Symptoms / Selected Lesions / Images), an `EMERGENCY ALERT` band between `===` rules, then `Top Probable Diseases` — three entries, each with Confidence, four match lines, and চিকিৎসা / ব্যবস্থাপনা / প্রতিরোধ / জরুরি করণীয় sections — closing on a Disclaimer. Rebuilt with semantic markup (new `.diag-card`/`.diag-key`/`.diag-rule`/`.diag-alert-title`/`.diag-section-title`/`.diag-disease`/`.diag-sub`) using real `<hr>`s in place of the original's typed `======` / `------` divider lines.
    - **Replicated a genuine bug**: entry 1 reads **"Confidence: 111% - High"**. Over 100% is clearly wrong in the source app's scoring, but it's what the recording shows, so it's transcribed verbatim rather than silently corrected to 100 — flagging it here so it reads as faithfulness, not a typo of ours. Worth raising with the client.
    - **Two gaps left unfilled rather than invented** — this screen is medical-advice content, so guessing is not acceptable: (a) disease 2 (Salmonellosis / Pullorum) has **no `⚠️ জরুরি করণীয়` section** in our build, because the sticky footer covered it in every frame and it was never visible; the other two diseases have all four sections, so the asymmetry is real, not an oversight. (b) The Disclaimer's last clause was cut mid-sentence at "…vet consultation / lab test"; completed as "করুন।" — a grammatical completion the sentence requires and that every parallel sentence in the app uses, not added content.
    - Result figures are the recording's captured run (Layer / 0-7 days / হঠাৎ মৃত্যু + ঝিমানো / Air sac cloudy) and **do not respond to what you tick** in the steps — the wizard's own checkboxes all ship unticked in their true default state, per the standing "show the screen as it first appears" convention.
    - New CSS in full: `.wizard-head`, `.check-card`/`.check-grid`/`.check-item`, `.wizard-nav`, `.upload-preview`/`.upload-note`, `.diag-card`/`.diag-key`/`.diag-rule` (+`.thin`)/`.diag-alert-title`/`.diag-section-title`/`.diag-disease`/`.diag-sub`/`.diag-sub-title`.

26. **Feed Formulation for Poultry** — `databank/feed-formulation.html` (landing) → `feed-select-type.html` (28-type picker) → `feed-formula-create.html` (the formula builder + Save modal), linked from the Data Bank list's 6th item. Matches structure from `WhatsApp Video 2026-08-04 at 8.18.11 PM.mp4` (87s, sampled at 1 fps).
    - **`feed-formulation.html`** — bare `.page-head` back arrow (the original has no header at all; arrow added per the standing hidden-nav rule), `.audit-hero` reused for the "Poultry Feed Formulation" title + subtitle, then four full-width tinted menu cards (new `.tint-card` with `.moss`/`.info`/`.gold`/`.indigo` variants): Create Feed Formula · Ingredient Library · Formula History · Nutrient Standard. The original's green/blue/orange/purple tints map onto existing tokens (`--moss-soft`, `--info-blue-soft`, `--gold-soft`, `--indigo-soft`) — **no new colours**. Only Create Feed Formula was tapped in the recording; the other three are inert.
    - **`feed-select-type.html`** — "Select Feed Type" + a live `Total Feed Types: 28` count, a search field, and a 2-column grid (new `.feed-grid`/`.feed-card`) of all 28 types, each card stacking a gray group label / bold bird name / green stage. The full 28 were read off three scroll positions: Broiler and Color and Sonali × Starter/Grower/Finisher (9), Layer × Chick/Grower/Pre-Layer/Layer (4), then Broiler Breeder, Color Breeder and Layer Breeder × Starter/Grower/Pre-Breeder/Breeder Phase-1/Breeder Phase-2 (15). Note the group label for all three breeder birds is just `Breeder`, not the bird name.
    - **The search box is wired** (`filterFeeds()`) — it hides non-matching cards and updates the header count. This is a display filter over recorded states, not a calculation: the recording shows `b` → 18 types and `boi` → 0 (blank grid, no "no results" message), and our build reproduces both exactly, which independently confirmed the 28-item transcription (only Broiler + the three Breeder birds contain a "b" — 3+5+5+5 = 18).
    - **`feed-formula-create.html`** — `.audit-hero` title + a new green `.crumb` line (`Broiler / Broiler / Starter`), then an "Add Ingredient" `.calc-card` (`<select>` + moss button), a "Formula Ingredients" card whose rows scroll inside their own box (new `.ing-list`/`.ing-row`/`.ing-name`/`.ing-meta`/`.ing-amount`, plus generic `.card-title`/`.card-note`), and the mint `.nutrient-card` readout. A new `.fab-stack`/`.fab-pill` floats four action pills over the content bottom-right — **100 kg** (`--info-blue`), **Balance** (`--action-teal`), **Result** (`--action-purple`), **Save** (`--moss`) — all four colours already existed as tokens.
    - **Ingredient data is fully captured, and it reconciles.** The 12 formula rows (Rice Polish 4.00 · Salt 0.30 · Soybean Oil 2.85 · Soybean Meal 38.00 · DL-Methionine 0.18 · Maize 50.40 · Toxin Binder 0.10 · DCP 1.60 · Wheat Bran 1.00 · Limestone 1.30 · L-Lysine 0.02 · Vitamin Mineral Premix 0.25) sum to exactly **100.00 kg**, and recomputing CP and ME from each row's own `CP % | ME | ৳/kg` meta gives **21.76%** and **2991.20 kcal/kg** — the recording's own displayed figures, to the decimal. That cross-check confirms every amount and every meta line was transcribed correctly. Cost (**৳53.75/kg**, **৳5375.35** total) is derived the same way from the captured prices. The remaining six rows — **Ca, Av.P, Lys, Met, Fiber, Fat** — are *plausible broiler-starter values, not captured*: the ingredient meta lines only expose CP/ME/price, so those can't be derived, and the recording's own on-screen values for them belong to a different (mid-edit) ingredient set. Flagged in an HTML comment on the page too.
    - **Add Ingredient appends a real row** (name + its captured meta + an empty `kg` amount) — the exact state the recording shows after adding Fish Meal and later Full Fat Soybean. The Live Nutrient Result deliberately **does not** recompute when you do this; that's the calculation we don't build. 100 kg / Balance / Result are inert for the same reason (and Result's output was never shown in the recording anyway). "Long press item to remove" is in the hint text but was never demonstrated — not wired.
    - ~~`Meat Bone Meal` is the one gap~~ — **closed by the Ingredient Library recording** (item 27): it's now `CP 48.0% | ME 2500.0 | ৳85.0/kg`, so all 15 ingredients carry real captured values and nothing on this screen is invented except the six nutrient rows noted above.
    - **Save** opens the recorded "Save Formula" modal (reusing `.modal-overlay`/`.modal-card`/`.modal-title`/`.modal-actions` + `.uline-field`), prefilled `Broiler Starter` with CANCEL/SAVE — both just close it.
    - **The picker passes the tapped feed type through a `?t=` query string** so the breadcrumb and the default save name follow the card you came from, instead of every one of the 28 landing on a hard-coded "Broiler / Broiler / Starter" (the flock-picker precedent). The save name follows the evidenced bird+stage rule (`Broiler` + `Starter` = `Broiler Starter`), which does read awkwardly for breeder types (`Layer Breeder Breeder Phase-2`) — kept, since the original's rule for those was never shown and changing it would be invention.
    - Never opened in this recording, so unbuilt at the time: **Ingredient Library** (since built — item 27), **Formula History**, **Nutrient Standard**, and the **Result** pill's output.
    - New CSS in full: `.audit-hero .crumb`, `.tint-card` (+`.moss`/`.info`/`.gold`/`.indigo`), `.feed-grid`/`.feed-card`, `.card-title`/`.card-note`, `.ing-list`/`.ing-row`/`.ing-info`/`.ing-name`/`.ing-meta`/`.ing-amount`, `.fab-stack`/`.fab-pill` (+`.info`/`.teal`/`.purple`/`.moss`), `.nutrient-card`/`.nutrient-total`/`.nutrient-list`/`.nutrient-cost`. No new tokens.

27. **Feed Formulation → Ingredient Library** — `databank/feed-ingredient-library.html` (list) → `feed-ingredient-add.html` (Add/Edit form), linked from the Feed Formulation landing's second tinted card (was inert, now built). Matches structure from `WhatsApp Video 2026-08-04 at 8.51.28 PM.mp4` (32s, sampled at 2 fps).
    - **`feed-ingredient-library.html`** — `.audit-hero` title + `Total Ingredients: 15`, a full-width moss **Add New Ingredient** button (reusing the existing `.page-action` wrapper), a search field, then all 15 ingredients as cards (new `.ing-card`): bold name, then **CP** (moss) and **ME** (info-blue) in two columns, then the price in rust bold underneath. The original's green/blue/orange trio maps straight onto `--moss`/`--info-blue`/`--rust` — no new colours.
    - **This screen closed the one open gap from item 26.** `Meat Bone Meal` — in the Add Ingredient dropdown but never used in a formula row, so previously rendered as `CP —% | ME — | ৳—/kg` — is confirmed here as **CP 48.0% | ME 2500.0 | ৳85.0/kg**, and `feed-formula-create.html`'s ingredient map was updated. It also independently re-confirmed all 14 other ingredients' figures (every value matches what was read off the formula rows) and the count of 15.
    - The count is left at the recording's **starting** state of 15. Mid-video a test entry (`rice`, values 2/3/2/3/2/6/2/3, price 25) is saved and the header ticks to 16 — not carried over, since it's obvious QA junk and the 15 real ingredients are the meaningful state.
    - The **search field is wired** to the same display-only filter as the feed-type picker. Searching was never demonstrated *on this screen*, but it's the same component in the same feature and a dead search box reads as broken in a demo — flagging it as a slightly looser evidence bar than usual.
    - **`feed-ingredient-add.html`** — "Add / Edit Ingredient", fields sitting **directly on the background with no white `.calc-card` wrapper** and **no labels, placeholder-only** (matching the original exactly; the same "don't assume every tool screen has the white card" case as `space-feeder-drinker.html`). Full-width Ingredient Name, then four `.field-row` pairs — CP % / ME kcal/kg · Calcium % / Av. Phosphorus % · Lysine % / Methionine % · Fiber % / Fat % — then full-width Price Tk/kg and a moss **Save Ingredient** button that returns to the list. Zero new CSS.
    - **This form confirms the app's full nutrient set** — CP, ME, Calcium, Av. Phosphorus, Lysine, Methionine, Fiber, Fat, Price — which lines up one-for-one with the Live Nutrient Result rows on `feed-formula-create.html`. It does **not** close that screen's synthesized-values note, though: the library cards still only display CP/ME/price, so per-ingredient Ca/Av.P/Lys/Met/Fiber/Fat were never shown anywhere and those six result rows stay plausible-not-captured.
    - Both screens hide the app-bar/bottom nav, so both get a `.plain-header`-style back arrow per the standing rule. The original shows no header on either.
    - New CSS in full: `.ing-card`/`.ing-card-stats`/`.ing-cp`/`.ing-me`/`.ing-price`. No new tokens.

28. **Feed Formulation → Formula History** — `databank/feed-formula-history.html` (list + Formula Details modal) → `feed-formula-edit.html`, linked from the Feed Formulation landing's third tinted card (was inert, now built). Matches structure from `WhatsApp Video 2026-08-04 at 9.15.10 PM (1).mp4` (26s, sampled at 2 fps).
    - **`feed-formula-history.html`** — `.audit-hero` title + `Total Saved Formulas: 1`, a search field, and one saved-formula card (new `.formula-card`, a `<button>` since the whole card opens a modal): bold name, gray `Broiler - Finisher | 03-08-2026 10:43 pm` meta, two moss stat lines, and the original's own faint `Tap: Details | Long press: Delete` hint. Long-press delete is named in that hint but never demonstrated, so it isn't wired.
    - Tapping the card opens the **Formula Details** modal (`.modal-card.scroll` + new `.formula-detail`): formula name / type / date, a bulleted list of all 12 ingredient amounts, a Result block, and a rust **Warnings** line (`Lysine is high: 1.05% | Target: 0.95%`). Actions are SHARE TEXT · SHARE PDF · EDIT (new `.modal-actions.three`). The two share buttons open the Android system share sheet in the original — inert here, same precedent as Export/Share PDF elsewhere in Data Bank.
    - **Everything on this screen reconciles arithmetically**, which is the strongest confirmation yet that the ingredient table transcribed in items 26–27 is correct. The 12 saved amounts sum to exactly **100.00 kg**; recomputing from each ingredient's Library figures gives **CP 19.19%**, **ME 3157.56 kcal/kg**, **৳52.44/kg** and **৳5243.93** total — all four match the recording's displayed values to the decimal, across a completely different formula from the one in item 26.
    - **`feed-formula-edit.html`** — reached from the details modal's EDIT. Prefilled with the saved Broiler Finisher amounts; otherwise the same skeleton as `feed-formula-create.html`.
    - **Two deliberate corrections to the original, at the user's request** (both flagged by them on seeing the recording):
      1. The real app reuses the Create screen verbatim for editing, so the header still reads **"Create Feed Formula"** while you are editing a saved formula. Retitled **"Edit Feed Formula"**, and the breadcrumb now leads with the formula's own name (`Broiler Finisher · Broiler / Broiler / Finisher`) so the screen states what it is acting on.
      2. The original's primary action reads **"Update"** with no object. Relabelled **"Save Changes"**.
      These are the first *intentional* departures from recorded UI in this feature — everywhere else the rule is to replicate what the app shows, including its bugs (cf. the `Confidence: 111%` transcription in item 25). Noting the distinction: that one was a wrong *value* worth surfacing to the client as-is; these two are wrong *labels* the client explicitly asked to have fixed.
    - Nutrient readout on the edit screen: Total, **CP 19.19%**, **ME 3157.56**, **Lys 1.05%** (off the details modal's own warning line) and both cost figures are captured; Ca, Av.P, Met, Fiber and Fat are plausible broiler-finisher values, since per-ingredient figures for those six are never displayed anywhere in the app.
    - Only **Nutrient Standard** and the **Result** action pill are now left unrecorded in this feature.
    - New CSS in full: `.formula-card`/`.formula-meta`/`.formula-stats`/`.formula-hint`, `.formula-detail` (+`.fd-head`/`.fd-warn`), `.modal-actions.three`. No new tokens.

29. **Feed Formulation → Nutrient Standard** — `databank/feed-nutrient-standard.html`, linked from the Feed Formulation landing's fourth tinted card (was inert, now built). Matches structure from `WhatsApp Video 2026-08-04 at 9.32.13 PM.mp4` (17s, sampled at 2 fps, plus a 10 fps re-extract of the 6.4–9.2s window — the list was scrolled fast enough through the Broiler/Color block that the 2 fps frames were all motion-blurred).
    - **`Total Standards: 28` — one target standard per feed type**, the same 28 the Select Feed Type picker lists. Search field, then 28 cards (new `.std-card`).
    - **Card design pass** (user asked for it right after the first build): the original renders each card as three run-on colour-coded text lines (`CP: 16.5%  |  ME: 2800.0 kcal/kg`, etc.), which is dense and hard to scan. Rebuilt as a header row — category **badge-circle** with a per-category emoji (🪺 Breeder · 🐔 Broiler · 🐓 Color · 🥚 Layer · 🐤 Sonali, tints rotating gold/rust/moss) + `<Breed> - <Phase>` + a `›` chevron — over the seven targets as **pill chips** (`.std-chips`/`.std-chip`), which is the design system's "chip-style pill tags for data values instead of plain text" move applied directly. The original's three-way colour grouping is preserved on the chips: moss = energy (CP, ME), info-blue = minerals (Ca, Av.P), rust = amino acids + fiber (Lys, Met, Max Fiber). No new colours. The category is still written out as text under the name, so nothing is carried by badge colour alone.
    - **Ordering is by category, not alphabetical by name** — all 15 Breeder entries first (Broiler Breeder ×5, Color Breeder ×5, Layer Breeder ×5), then Broiler ×3, Color ×3, Layer ×4, Sonali ×3. Worth noting because a plain A–Z sort would have put `Broiler - Finisher` before `Broiler Breeder - …`; the app doesn't do that.
    - Tapping a card opens the **Nutrient Standard** detail modal — Category / Breed / Phase, then the same seven figures spelled out in full (`Target CP`, `Target ME`, `Target Calcium`, `Target Available Phosphorus`, `Target Lysine`, `Target Methionine`, `Max Fiber`) and an OK button. Driven from each card's own `data-*` attributes by a small `openStd()` helper rather than 28 hand-written modals — display only, nothing derived.
    - **Independently cross-checked against a different recording**: this screen's `Broiler - Finisher` target Lysine is **0.95%**, which is exactly the target quoted in the Formula History warning on the saved Broiler Finisher formula (`Lysine is high: 1.05% | Target: 0.95%`, item 28). Two separate recordings agreeing on the same number is good evidence the table is transcribed correctly.
    - The 28 cards are generated from a checked data table rather than hand-written, so the chips, the header and the modal's `data-*` attributes all come from one source and can't drift apart. Verified back out of the built file: 28 cards, category counts Breeder 15 · Broiler 3 · Color 3 · Layer 4 · Sonali 3 = 28, and all 196 chip values + 56 header fields matching their own data attributes.
    - Search is wired to the usual display-only filter, matching the card title so the placeholder's own examples (`Broiler`, `Layer`, `Starter`) all work. Searching was never demonstrated on this screen — same looser evidence bar noted for the Ingredient Library in item 27.
    - **This completes the Feed Formulation feature** — all four landing cards are now live. The only thing still unrecorded anywhere in it is the formula builder's **Result** action pill.
    - New CSS in full: `.std-card`/`.std-top`/`.std-head`/`.std-cat`/`.std-chev`, `.std-chips`/`.std-chip` (+`.energy`/`.mineral`/`.amino`). No new tokens.

30. **Feed Formulation — density pass** (2026-08-04, user: *"there are a lot of wasted space in the screens under Feed Formulation for Poultry"*). Applies across every screen in the feature (Nutrient Standard was built afterwards and follows the same conventions); no content changed, layout only.
    - **Top block tightened on every screen** — this was the actual ask. The back-arrow row and the centred hero each carry generous padding that stacks into a large empty band above the content. Added **scoped** `.page-head.tight` (`16px 16px 4px` → `8px 16px 0`) and `.audit-hero.tight` (`22px 20px 6px` → `4px 20px 2px`, h1 `24px` → `21px`), so the shared defaults the Bio-Security / CAPA / Lab Report / Disease-ID screens depend on are untouched. ~40px reclaimed per screen.
    - **A first attempt overshot and was reverted**: the landing's menu and the Add-Ingredient form were made to stretch to full screen height to kill the dead band under them. User: *"i did not mean that you stretch the cards to get full height. just tighten up some spaces on the top of the screen."* Both reverted to natural height. **Standing lesson: "wasted space" means tighten the spacing, not stretch content to fill the frame** — a short screen ending in empty space is normal and fine.
    - Per-item density (kept): landing rows gained an icon badge + chevron laid out across the width (the feature had none of the design system's signature badge treatment before); feed-type cards got a micro-caption group label and a moss pill chip for the stage; Ingredient Library cards went from three lines to two (name + price share a row, CP/ME below); the formula builder's ingredient `<select>` and its button sit side by side, and the Live Nutrient Result became a 2-column label/value grid instead of eight stacked text lines.
    - **One functional fix along the way**: the four action pills on the builder were a floating vertical stack pinned right, which sat permanently on top of the nutrient readout's right-hand column — ME, Av.P, Met, Fat and the total were all unreadable behind them. Moved to a sticky bottom row (same pattern as the existing `.wizard-nav`). This is a deviation from the original's floating stack, made because the original's own layout hides its own data at this screen width.

31. **Breeder Development Monitor** — `databank/breeder-monitor.html` (dashboard) → `breeder-flock-create.html`, `breeder-weekly-input.html`, linked from the Data Bank list's 5th card (was inert, now built). Matches structure from `WhatsApp Video 2026-08-04 at 21.40.56.mp4` (53s, sampled at 2 fps).
    - All three screens **hide the app-bar and bottom nav**, so all three get a `.page-head tight` back arrow per the standing rule (dashboard → `index.html`, the two forms → the dashboard). The original shows no header bar on any of them.
    - **`breeder-monitor.html`** — `.audit-hero tight` title + subtitle ("Track growth, stage, weight and uniformity with smart alerts"), a `.calc-card` "Select Flock" `<select>`, then a `.calc-btn-row` pair: moss **+ Add Flock** / gold **+ Weekly Input** (the original's green/orange, straight onto existing tokens). Below that, **eight collapsible report sections** — Flock Summary · Male and Female Status · Development · Management Action · Alert · Weekly History · Weight Graph · Uniformity Graph.
      - New accordion component (`.acc-sec`/`.acc-head`/`.acc-body`/`.acc-card`), distinct from the Bio-Security `.menu-card` accordion: an `--indigo-soft` header bar with a rotating ▼ over a white body card. **Non-exclusive** — the recording opens and closes sections independently, unlike `toggleMenu()`'s exclusive-open. Recorded default is Flock Summary/Status/Alert open and the rest collapsed; **ours ships all eight open** per the standing "every element visible for design review" convention. Toggling is a state swap, not a calculation.
      - `.mf-grid`/`.mf-card` — the side-by-side Female (pink) / Male (blue) status pair, mapping the original's pink/blue onto `--rust-soft` / `--info-blue-soft`. Five rows each: Target · Actual · Status · Uniformity · Uni Status.
      - Alert reuses the existing `.notice` component as-is (amber box, `#8A5E14` bold text) — an exact match for what the original renders, zero new CSS.
    - **The recorded flock had no weekly data at all** — Actual 0.0 g, uniformity 0.0%, an empty Weekly History, and a blank Weight Graph — so the whole bottom half of the screen was unreviewable as recorded. Per the standing "ship the result section pre-filled with realistic example data" rule, the dashboard ships a **populated flock** instead: four weekly rows (W1 104 g/79.8% · W2 188 · W3 272 · W4 385 g/84.9% female; 126 · 230 · 342 · 496 g/86.4% male) feeding the Weekly History table, both charts, and the status pair. Everything **structural** — section names, field names, row order, the Development/Management Action sentences, Age `4 week 6 day`, Female Stage `Starter` / Male Stage `Starter Male`, and the Female 390.0 g / Male 490.0 g targets — is transcribed verbatim from the recording; only the actuals and history are synthesized.
      - Two status labels are **generic-scale completions, not captured**: `On Target` (weight) and `Good` / `Warning` (uniformity). The recording only ever showed the failing tier (`Critical Underweight`, `Critical`) because the flock had no data. The uniformity tiers are anchored by the chart's own threshold lines (Target 85%, Warning 80%) — same looser evidence bar already noted for Molting's Low/Medium/High dropdowns.
      - Alert text keeps the recording's own phrasing where it applies ("Female uniformity low. Grade flock and adjust feeding.") and adds one line matching the synthesized data.
    - **Two real charts**, so the `dataviz` skill applies properly for the first time since the Lab Report titer graph (that one was a single point; these are 2 series × 4 points):
      - Series are sex-coded to match the status cards above — female `--rust`, male a **new `--chart-blue:#2A6E9E`**. `--info-blue` was tried first and **failed the validator's chroma floor** (0.078, "reads gray") as a 2px stroke; `#2A6E9E` is the same hue with the chroma lifted and passes all six checks against a white surface (worst adjacent pair ΔE 16.6 protan / 25.5 normal). Verified with `scripts/validate_palette.js`, not by eye.
      - **The Uniformity Graph's y-axis is 70–100, not the original's 0–100.** On a 0–100 axis all four series values, the 85% target line and the 80% warning line collapse into a 10px band at the top of the plot — the original's own chart is unreadable for that reason. Deliberate, flagged deviation.
      - The threshold lines are dashed and **written out** (`Target 85%`, `Warning 80%`), so no meaning rides on colour alone; target keeps the original's green (`--moss`) but warning moved from red to `--gold`, since `--rust` is already the female series on that same chart. Both charts carry a legend + a direct label on the last point only (never a number on every point), and the Weekly History table right above them is the table view of the same numbers.
    - **`breeder-flock-create.html`** — "Create Breeder Flock": `.uline-field` underline-only inputs with no labels and no white card wrapper (same shape as `feed-ingredient-add.html`) — Flock Name, breed `<select>` (**BROILER_BREEDER / LAYER_BREEDER / COLOR_BREEDER**, all three confirmed from an opened-dropdown frame), Start / Hatch Date, a Female Count | Male Count `.field-row`, Shed / House, Remarks, then a gold **SAVE FLOCK**. The date field is a plain typeable input — the original opens the same native Android date picker already documented for `flock-create.html`, not rebuilt again. New CSS: one line, `.field-row .uline-field{flex:1;min-width:0}`.
    - **`breeder-weekly-input.html`** — "Weekly Weight and Uniformity", with the recorded Bengali gating line verbatim (`১ সপ্তাহ পূর্ণ হয়নি। Weekly input এখন দেওয়া যাবে না।`). Female section → Female Weight g | Uniformity %, Male section → Male Weight g | Uniformity %, then Remarks and a gold **SAVE WEEKLY DATA**. The section labels are plain gray in the original; here they take `.form-section-title.rust` / `.moss` to echo the dashboard's female/male colour coding. **Note the mismatch left in deliberately**: that Bengali line belongs to a 3-day-old flock, while the dashboard's selected flock is 4 wk 6 d — it's the only subtitle text the recording gives for this screen, so it's transcribed rather than reworded.
    - **Flock names replaced with meaningful ones**, same precedent as the Flock Data Management flow: the recording's `mahmudul` and `name` (typed live on camera, alongside shed `faruk`) became **Layer-PS1** and **Broiler-PS2**. All figures are untouched.
    - Never recorded, so unbuilt: the Select Flock dropdown was never opened (two flocks are known only from the closed field before and after the create flow), and no weekly data was ever saved, so the post-save populated state of any section is ours, not the app's.
    - **Upgraded design pass, immediately after the functionality-match build** (user: *"this is not looks good... you made it as like the video"* — the original screens are flat tinted bars over flat text blocks, and copying that verbatim looked unfinished). Same data throughout, restyled only:
      - **Flock hero replaces the Select Flock card + the Flock Summary section.** The original's "Flock Summary" only restated the flock already named in the selector, so the two merged into one `.flock-hero` card: badge-circle 🐔 + flock name + type, the `<select>`, and Age / ♀ Stage / ♂ Stage as `.std-chip` pills (the design system's "chip-style pill tags for data values" move). Eight sections became seven.
      - **Section cards**: the floating `--indigo-soft` bar over a separate white card became one white shadowed card per section with an emoji icon badge (`.acc-ico`, tints rotating gold/rust/moss/indigo) + title + chevron — the same treatment the Bio-Security accordion got in item 22. The old `.acc-card` inner wrapper was dropped (white on white).
      - **Male/Female status** went from two blocks of five `label: value` text lines to two cards each with a sex badge, the actual weight as a big Baloo number, its target underneath, a **meter bar** filling actual-against-target, and two `.tag-pill` chips (weight status + uniformity + its status). This is the single biggest change; the recorded version is a wall of small text.
      - Development / Management Action rows now lead with a ♀ / ♂ badge instead of the literal `"Female: "` / `"Male: "` prefix; Alert became one tinted `.alert-row` per alert with its own icon instead of one amber blob of bold text; Weekly History went from 3 merged columns to a 5-column `.data-table.compact` (weight bold, percentages muted).
      - Charts gained a title-plus-legend header row and a 7%-opacity area fill under each weight series. Colours, axes and thresholds are unchanged from the validated build above.
      - Both forms were rebuilt from bare `.uline-field` underline inputs on a flat background into a white `.calc-card` with `.form-section-title` + emoji section headers (Flock Identity 🐔 / Starting Counts 🔢; ♀ Female / ♂ Male) and a bold `.form-label` over every input — same treatment `molting-add.html` got in item 21. `breeder-weekly-input.html`'s Bengali gating line moved from a gray subtitle into the existing `.notice` box, with a flock-context line (`Layer-PS1 · LAYER_BREEDER · Week 5`) added above it.
    - New CSS in full: `.flock-hero`/`.flock-hero-top`/`.flock-hero-id`/`.flock-name`/`.flock-type`, `.acc-sec`/`.acc-head`/`.acc-ico` (+`.gold`/`.rust`/`.moss`/`.indigo`)/`.acc-title`/`.acc-arrow`/`.acc-body`, `.sex-grid`/`.sex-card` (+`.f`/`.m`)/`.sex-head`/`.sex-badge`/`.sex-metric`/`.sex-target`/`.sex-chips`, `.meter`, `.sex-note`, `.alert-row`, `.data-table.compact`, `.chart-head`/`.chart-legend`/`.legend-item`/`.legend-swatch`, `.chart-card .line`/`.area`/`.dot`/`.ref-line`/`.ref-label`/`.point-label`, `.chart-caption`, `.field-row > .fr-col`. New token: `--chart-blue`.

32. **Investment Calculator and Project Planning** — ten files, linked from the Data Bank list's 8th card (was inert, now built): `databank/investment-dashboard.html` → `investment-project-new.html` / `investment-project.html` → `investment-fixed-capital.html`, `investment-working-capital.html`, `investment-market-price.html`, `investment-mortality.html`, `investment-profit-roi.html`, `investment-final-report.html`, `investment-cash-flow.html`. Matches structure from `Investment Calculator and Project Planning.mp4` (133s, sampled at 2 fps → 266 frames). **This completes the Data Bank — all nine tools are now built.**
    - Every screen hides the app-bar and bottom nav, so every screen gets a `.page-head tight` back arrow per the standing rule (dashboard → `index.html`, everything else → its real parent). The original has no header bar anywhere in this feature.
    - **Built in the house style from the start, not as a literal copy** — following the lesson from item 31's design pass. The original's teal/orange/green full-width section bars became the same `.acc-*` white-card accordion the Breeder Monitor uses; its walls of green cost text became `.cost-row` label/value pairs; its bare unlabelled underline inputs became labelled `.calc-input` fields inside `.calc-card`s; its project facts became `.std-chip` pills. **No new colours** — the teal maps to the existing `--action-teal` on the one Export / Share PDF button, everything else onto moss/gold/rust/indigo.
    - **`investment-dashboard.html`** — `.stat-grid` (Projects 2 · Bird Capacity 3,000), a gold **+ New Project** button (the original floats it as a teal FAB; made a normal full-width action, same call as the Feed Formulation density pass), then the saved projects as `.record-card` links: badge circle + name + created date, facts as chips, and a `No Profit Forecast Saved` `.tag-pill` (the original renders that as a green tinted bar). The recorded ⋮ **Edit / Delete** menu is wired as a `.modal-overlay`.
    - **`investment-project-new.html`** — Project Name, then five `<select>`s and two number fields. Option lists read off opened-dropdown frames: **Farm Type** (Broiler / Color / Sonali / Layer / Broiler Breeder / Layer Breeder / Color Breeder), **Breed Type** (Cobb 500 / Ross 308 / Hubbard / Indian River / ISA Brown / Lohmann / Hy-Line / Sonali / Color Bird), **House Type** (Open House / Control House), **Floor System** (Deep Litter / Slat System / Layer Cage). **District ships all 64 Bangladesh districts** — the recording only showed the first seven (Bagerhat → Brahmanbaria) plus Munshiganj on the other project, but that is exactly the standard alphabetical 64-district list, so completing it is a public-fact completion rather than invented app data. Flagged since it's a looser bar than the breed lists above, which are transcribed exactly.
    - **`investment-project.html`** — the project hub: a `.proj-card` (badge + name + created date + all seven project facts as chips) over a single `.menu-card` of seven rows — Fixed Capital · Working Capital · Market Price Update · Mortality Simulation · Profit and ROI Forecast · Final Investment Report · Cash Flow Forecast — then the teal Export / Share PDF button (inert, same precedent as every other share/export button in Data Bank).
    - **`investment-fixed-capital.html`** — three sections. Farm Area Requirement (facts + Required Shed Area 3,000 sqft / Extra Utility 1,500 sqft / **Total Land 10.33 decimal**); Equipment and Automation (three live `<select>`s — Feeding: Manual/Pan/Chain, Drinking: Manual Drinker/Nipple System, Layer Cage: Local/Imported — over the recorded readout, **Estimated Equipment Cost 445,000**); Fixed Capital Cost (a manual-edit toggle + eleven fields + the breakdown, **Total Fixed Capital 4,067,558**).
    - **`investment-working-capital.html`** — Working Capital Auto Summary (35-day cycle, 2.8 kg feed/bird, 5,600 kg total, 2 labour, 35,000 labour cost, 11,667 electricity+diesel, chick 55, feed 60) and Working Capital Cost (toggle + eleven fields + breakdown, **Total Working Capital 552,667**).
    - **Field labels on the two capital screens are derived, not read.** The original shows those input stacks with **no labels at all** — just underlined numbers, never empty, so no placeholder text was ever visible either. Rows 3–11 of each stack map one-for-one onto the result list printed directly below them, which is solid; the first two of each are pinned by arithmetic (10.33 decimal × 100,000 = Land Cost 1,033,058; 3,000 sqft × 700 = Shed Construction 2,100,000; 2,000 birds × 55 = Chick cost 110,000; 5,600 kg × 60 = Feed cost 336,000). Both derivations are noted in HTML comments on the pages themselves.
    - **`investment-market-price.html` is the one screen left unlabelled**, deliberately. Fifteen bare price fields (55·55·55·55·700·60·60·60·55·60·180·260·260·10·18) with no labels anywhere in the app and no cross-screen anchor for ten of them — naming which bird type each price belongs to would be inventing domain facts, which the medical/breed rule forbids. Shipped as recorded inside a proper card, with a `.card-note` saying the app itself ships no labels. **Worth asking the client for this screen's field list.**
    - **`investment-mortality.html`** — two derived-label inputs (Market Price/kg 180, Average Body Weight 2 kg — fixed by the table's own arithmetic, 1,980 × 2 × 180 = 712,800) and the twelve-row scenario list rebuilt as a `.data-table.compact` (Mortality / Live Birds / Expected Revenue) instead of the original's stacked text blocks. Rates are 1–10% in steps of 1, then 12% and 15% — that skip is the app's, not a truncation.
    - **`investment-profit-roi.html`** — investment summary card, five forecast inputs (Mortality 5% · Market Price 180 · Body Weight 2.0 kg · Other Income 0 · Other Cost 0 — the first three fixed by the outputs, the two zeros never named anywhere so they carry generic labels), and the four results promoted to `.stat-tile`s: **Expected Revenue 684,000 · Net Profit 131,333 · Profit/Bird 66 Tk · ROI 2.84%**. All four reconcile: 684,000 − 552,667 = 131,333; 131,333 ÷ 2,000 = 66; 131,333 ÷ 4,620,225 = 2.84%.
    - **`investment-final-report.html`** — project chips, a `.result-hero` for **Grand Total Investment 4,620,225 Tk**, the Fixed/Working split and Per Bird 2,310 / Per Shed 2,310,113, then Bank Loan Estimation (70% 3,234,158 / 30% 1,386,068) and the app's own closing note in a `.notice`.
    - **`investment-cash-flow.html`** — summary card ending in **Final Operational Balance 0 Tk**, plus the recorded gating message (`Please save Profit Forecast first`) as a `.notice`. Left at 0 rather than filled with a synthesized figure, because the zero *is* the recorded state and the message explains it — the "pre-fill the result" convention doesn't apply to an output the app is deliberately withholding.
    - **Project names replaced with meaningful ones**, same precedent as items 19 and 31: `project 2` → **Bandarban Broiler Project**, `Project one` → **Munshiganj Broiler Project** (each named for its own district). Every figure is untouched.
    - New CSS in full: `.cost-list`/`.cost-row`/`.cost-total`, `.switch-row`/`.switch-label`/`.switch` (pure-CSS toggle off the existing `.sr-only`), `.proj-card`/`.proj-card-top`/`.proj-name`/`.proj-sub`. No new tokens. Everything else reuses `.acc-*`, `.menu-card`/`.menu-row`, `.stat-grid`, `.std-chips`, `.data-table.compact`, `.result-hero`, `.tag-pill`, `.notice`, `.modal-*`.

33. **কমিউনিটি (Community / Posts)** — `community/index.html` (feed) + `community/create-post.html`, and a **sixth bottom-nav tab** added between ডাক্তার and ডাটা ব্যাংক. Matches structure from `WhatsApp Video 2026-08-05 at 05.55.29.mp4` (25s, sampled at 2 fps).
    - **The source recording is a different app**, not our client's — a green agri app the user sent as a *feature reference* ("this is the community feature screen"), not a screen to clone. Its home screen mixes the post feed with that app's own widgets (a 3-day weather strip, Consultation/Services buttons, a Daily Market Price row, a chat FAB, and a Home/Market Price/Market Place/Notification/Profile nav). Only the **community feature itself** — the composer, the post feed and Create Post — was built; the rest belongs to that app, not ours. This is the first screen built from a reference app rather than from the client's own app.
    - **Nav is now six items** — হোম · কিনুন · বেচুন · ডাক্তার · **কমিউনিটি** · ডাটা ব্যাংক — inserted in all nine files that carry a bottom nav (`index.html`, `buy/`, `sell/`, `doctor/`, `databank/index.html`, `flock-data-management.html`, `lab-report.html`, `lab-report-detail.html`, `vaccination-schedule.html`). `.bottom-nav` padding tightened to `10px 4px` and `.nav-item` given `flex:1; min-width:0` so six Bengali labels fit; verified in-browser across all nine — widest label (ডাটা ব্যাংক) is 41px, nothing clips.
    - **`community/index.html`** — a nav-level destination, so it keeps the app-bar + bottom nav and has no back arrow. A `.composer` card (avatar badge + pill field + gold image button) links to Create Post, then four `.post-card`s: avatar badge (rotating gold/rust/moss per the standing no-real-photos rule — the original shows real profile photos), name + verified tick, relative time + audience, a `⋯` menu, the post body, a like/comment/share count row, and a divided action row. Post text and counts are transcribed from the recording (MD SAHIN HOSSAIN · Shahidul Islam · Md Aminur islam Babu · Md Ebrahim); the fourth post's body was cut off mid-scroll at "A farmer lifestyle" and is kept exactly that short rather than extended.
    - **`community/create-post.html`** — new `.create-head` bar (moss, a circled ✕ on the left and a gold primary action on the right — a fifth header pattern, since `.subpage-header`'s centred back-arrow layout can't carry a right-hand action), author row with an `.audience-pill` (🌐 সবাই ▾), a full-height `.post-textarea`, and a four-button `.attach-row` pinned under it (ছবি / ফাইল / ট্যাগ / ক্যামেরা — the original's bottom toolbar is four unlabelled icons; labels added since unlabelled glyphs aren't reviewable). All four are inert, same precedent as the image upload on `disease-id-step4.html`.
    - **UI chrome is Bengali, the reference app's is English.** Like/Comment/Share/Create Post became লাইক/মন্তব্য/শেয়ার/পোস্ট তৈরি করুন, and counts use Bengali numerals, to match this app's own Bengali nav and home screens. Post *content* stays exactly as recorded (including the English "price of white egg?" and "A farmer lifestyle").
    - **The ⋯ menu was redesigned as a bottom sheet** (user, right after the position fix above: *"design should be fix"*). A centred `.modal-card` is the app's confirm-dialog pattern — it reads as "are you sure?", which is wrong for a post overflow menu, and the source recording's own share action opens a bottom sheet. New `.sheet-overlay`/`.sheet` component: rounded top corners, a grab handle, a header carrying the poster's monogram avatar and name so the sheet states which post it acts on, then four icon-badge rows (🔖 সেভ · 🔗 লিঙ্ক কপি · 🔕 কম দেখান · 🚩 রিপোর্ট, the last in rust as the destructive option) and a filled বন্ধ করুন button. `openPostMenu()` now takes the poster's initials and tint too, so the sheet header matches the card it came from. Backdrop tap closes it. Verified scrolled to the last post: sheet pins to the viewport bottom (375×398, bottom = 736 = viewport height), header/avatar swap per post, and all three close paths clear it.
    - **Like / মন্তব্য / শেয়ার are now live** (user: *"like, comment, and share not working"*). All three are UI-state work, not calculation, so they sit on the right side of the standing static-mockup line — the same basis as the ROI radio toggle and the feed-type search filter.
      - **Like** toggles per post and the count follows. Each card carries `data-likes` (the count *excluding* you) and `data-liked` (your own state), so liking and unliking never recompute from a running total and can't drift. The engagement line re-renders between the moss-soft `.like-pill` and a plain "এখনো কোনো লাইক নেই" as the count crosses zero, and counts render in Bengali numerals.
      - **মন্তব্য** opens a new `.sheet.tall` comment sheet — post author in the header, a scrolling list, and an input row with a send button (Enter works too). **Comments start empty on every post**, because the recording shows `0 comments` throughout; nothing was seeded, so the sheet opens on a real empty state and fills only with what you type. Posting appends the comment, bumps the card's count and re-renders both. Comment text is written with `textContent`, never `innerHTML`, so typed markup can't inject into the page — verified with an `<img onerror>` payload.
      - **শেয়ার** opens a share sheet (WhatsApp / Facebook / Messenger / লিঙ্ক + a "কমিউনিটিতে শেয়ার করুন" row). Picking any option closes it, increments that post's share count and confirms with a toast. The original hands off to the Android system share sheet, which is OS UI — an in-app sheet is the equivalent that a static demo can actually show.
      - `.toast` was also changed from `position:absolute` to `fixed` (centred, max-width 343px) — same latent bug as the modals: on a page taller than the phone frame it was landing at the bottom of the *document* instead of the viewport. Also fixes it on `vaccine-breed-schedule.html`, the only other page using it.
      - Verified end-to-end in-browser: like on/off/on across two posts including the 0→1 pill swap, two comments posted (count `২টি মন্তব্য`, card reads `২ মন্তব্য`), blank input ignored, markup escaped, share incrementing to `১ শেয়ার`, every sheet pinned to the viewport bottom, and backdrop/cancel/row closing all three.
    - Not recorded, so not built: post detail, the ⋯ menu's real contents (ours offers সেভ / লিঙ্ক কপি / কম দেখান / রিপোর্ট — plausible, flagged), and Share (the recording opens the Android share sheet, which is system UI, not app UI).
    - **Upgraded design pass, same session** (user: *"current design not looking good so far"*, scoped to the community screens only, direction left to me). Same content throughout, restyled:
      - **`.comm-hero`** — a full-bleed moss→moss-dark gradient band with a rounded bottom, the section title and a one-line description, and the gold circle motif borrowed from `.banner-card`. The feed previously opened straight into the composer under a generic app-bar with nothing naming the screen.
      - **The composer is lifted onto the hero** (`margin-top:-24px` + a deeper shadow) so the two read as one unit — the signature overlap move, and it kills the flat "stack of identical boxes" look the feed had.
      - **Monogram avatars replace the emoji badges.** `.avatar` is a solid gold/rust/moss/info-blue circle carrying the poster's initials in Baloo 800. Emoji-in-a-circle is the standing stand-in for *missing photography*, but on a social feed every card wore one and it read as unfinished; initials read as a deliberate design choice and still need no assets. The rest of the app's emoji badges are untouched.
      - **Engagement was restructured**: the old three-count row spread across the full width became a moss-soft `.like-pill` on the left ("👍 ২ জন") against muted comment/share counts on the right, and the post with no likes says so in words instead of showing a bare `০`. The action row's buttons are now pill-shaped with the liked state filled moss-soft, rather than three identical grey text labels.
      - The verified tick became a filled moss circle rather than a bare green glyph, name/body sizes were bumped for hierarchy, and `⋯` now aligns to the top of the card.
      - **Create Post** was rebuilt around a real `.calc-card` instead of bare elements on the background: author row, a working audience `<select>` (`.audience-select` — সবাই / শুধু বন্ধুরা / শুধু আমি) in place of the static pill, the textarea, and a new dashed `.drop-zone` filling what was a large blank area with an actual affordance ("ছবি বা ভিডিও যোগ করুন"). The four attach buttons kept their tinted icons but are now rounded squares.
      - Verified in-browser on both screens: no overflow, no clipped text, every component at its intended size.
    - **Modal positioning bug found and fixed while verifying the `⋯` post menu** (user asked for it to be checked). The button itself worked — all four wired, title swapping per post, both close paths clearing — but the dialog it opened could land **off-screen**. `.modal-overlay` was `position:absolute; inset:0` against `.screen`, and `.screen` has no fixed height: it grows to its content (the community feed is ~1300px tall), so the dialog centred halfway down the *page* rather than the viewport. Tapping `⋯` on a post near the bottom put the dialog above the fold — measured at `top: -86px`. Changed `.modal-overlay` to `position:fixed` and capped `.modal-card` at `max-width:347px` so it still reads as a phone-width dialog on a wide window. **This was a latent bug on every long screen with a modal, not just this one** — re-verified all 12 modals across 7 pages (community, flock-list, flock-data-management ×2, vaccine-breed-schedule ×4, lab-report, investment-dashboard, feed-formula-history) scrolled to the very bottom: all now stay inside the viewport. `capa-tracker.html`'s own `.capa-modal-overlay` was already `position:fixed`, which independently confirms the fix direction.
    - New CSS in full: `.comm-hero`, `.avatar` (+`.sm`/`.gold`/`.rust`/`.moss`/`.indigo`), `.composer`/`.composer-field`/`.composer-icon`, `.post-card`/`.post-head`/`.post-id`/`.post-name`/`.post-verified`/`.post-meta`/`.post-more`/`.post-body`/`.post-engage`/`.like-pill`/`.engage-meta`/`.post-actions`/`.post-action` (+`.on`), `.create-head` (+`.close-btn`/`.post-btn`), `.compose-author`, `.audience-select`, `.post-textarea`, `.drop-zone` (+`.dz-ico`), `.attach-row`/`.attach-btn`/`.attach-ico`, plus the six-item `.bottom-nav`/`.nav-item`/`.nav-label` tweaks. No new tokens.

34. **কিনুন (Buy) — full e-commerce section**, 7 pages under `buy/` plus `assets/js/shop.js`. `buy/index.html` (catalog) · `product.html?id=` (detail) · `cart.html` · `checkout.html` · `order-success.html?id=` · `orders.html` · `order-detail.html?id=`.
    - **No recording exists for this section** — the client brief names a Buy/Sell marketplace as a core feature, but nothing was ever captured. This is the **first screen designed from scratch** on the established design system rather than transcribed from source frames. Catalog contents, prices, sellers and copy are all synthesized; treat every product fact here as placeholder until a real catalog arrives. (Community, item 33, was built from a *different app's* recording — this one has no reference at all.)
    - **Decisions taken with the user before building**: design from scratch · full flow through order tracking · **live cart with real arithmetic** · catalog limited to poultry equipment + feed/supplements · **ASCII numerals for money** (৳1,250) with Bengali numerals kept for dates and item counts · cart badge on the seven `buy/` pages only · **no Home-screen entry point** — Buy stays reachable through the 🔍 কিনুন nav tab exactly as before, so `index.html` is untouched.
    - **This is the one place in the project where JS computes a number, and it is a deliberate, user-approved exception to the "static mockup, no working logic" rule.** A basket whose total never moves is unusable as a demo, and cart arithmetic is not the same class of thing as the agronomy calculators the rule exists to protect. **The rule still binds everywhere else** — do not treat this as precedent for the `accounting/` or `production-cost/` tools. Everything else in the standing rules was followed: no new colour tokens, emoji badges instead of photography, no `disabled` inputs, result sections pre-filled with realistic data.
    - **`assets/js/shop.js`** — the project's **second** shared JS file (after `app.js`). Holds the 22-product catalog as the single source of truth, so grid/detail/cart/orders all render from one array instead of the same 22 products being copy-pasted into seven HTML files. Exposes three globals: `Shop` (catalog, `money()`, `qs()`, `stars()`, `dateLabel()`/`dateInDays()`, `toast()`, `paintBadge()`), `Cart` (`add`/`setQty`/`remove`/`count`/`subtotal`/`lines`/`shippingFee`/`clear`), `Orders` (`all`/`byId`/`place`/`cancel`/`statusOf`/`seed`). Storage keys `eti_cart` and `eti_orders`.
      - **Every storage access is wrapped in try/catch with an in-memory fallback** — Safari throws `SecurityError` on `localStorage` over `file://`. The pages still render there; only cross-page persistence degrades. **Serve over http:// when demoing** (`python3 -m http.server`), or use Chrome.
      - `Cart.read()` drops lines whose product id is no longer in the catalog, so a stale basket can't break a page after the catalog changes.
      - `Orders.seed()` writes three demo orders on first visit (one shipped, one delivered, one cancelled) so My Orders is never blank — the same rule the calculator result sections follow.
    - **Catalog — 36 products**, tints rotating gold → rust → moss: 14 equipment (feeders, drinkers, brooders, incubators, cages, tools) · 8 feed/supplement · **5 বাচ্চা · 3 লেয়ার পুলেট · 6 ভ্যাকসিন** (added in a follow-up turn on the user's request). Twelve category chips, live birds and consumables first because those are the repeat purchases: সব · বাচ্চা · লেয়ার পুলেট · ফিড · সাপ্লিমেন্ট · ভ্যাকসিন · ফিডার · ড্রিংকার · ব্রুডার · ইনকিউবেটর · খাঁচা · যন্ত্রপাতি. Delivery is রেগুলার ৳120 (৩–৫ দিন) / এক্সপ্রেস ৳250 (১–২ দিন), regular waived over ৳5,000. Payments: ক্যাশ অন ডেলিভারি · বিকাশ · নগদ · রকেট · কার্ড.
    - **Breed and vaccine names are reused from this app, not invented** — per the standing rule that specific domain facts (breed names, disease names) are not a thing to synthesize. Chicks/pullets use Cobb 500 and Ross 308 (already on the broiler standard-data screens), Hy-Line Brown (layer), Color SA51A (Sonali), Muscovy Duck (duck); the six vaccines are exactly the ones the Vaccination Schedule module already names — ND Live, ND Killed, IBD Live, IB Live, Marek's, Fowl Pox — with the routes it records (ND Live and IB Live eye drop, Marek's SC). Chick prices anchor to Home's market-rate row (IR ৩৪৳ / Color ৬০৳ / Layer ৩০৳). **Every vaccine description ends by deferring dosing to the vial insert** — a mockup must not be the thing a farmer doses from.
    - **Batch minimums** (`minQty` / `step` on a product, defaulting to 1/1) — a chick line that says "ন্যূনতম 500 বাচ্চা" has to actually be orderable at 500. Chicks start at 500 stepping by 100 (Sonali 300, duck 100/50), pullets at 50 stepping by 10; everything else is unchanged at 1-at-a-time. The detail stepper opens at the minimum and shows the rule under the পরিমাণ label, the catalog card prints "· ন্যূনতম 500" so ＋ can't silently drop 500 chicks in the cart, the toast names the quantity when it's a batch, and stepping *below* the floor in the cart opens the remove confirmation rather than snapping back up. `Cart` clamps through `Shop.clampQty()`; the old hard cap of 99 became `MAX_QTY = 20000`.
    - **The cart badge counts distinct lines, not units** (`Cart.lineCount()`), capped at "99+". It counted units until batch products landed and a 500-chick order put "500" inside a 17px bubble. `Cart.count()` still returns total units.
    - Min-order figures render in **ASCII everywhere they appear** — spec row, description prose and stepper hint — because the stepper value itself is ASCII and the same fact must not appear twice in two numeral systems on one screen. Descriptive specs (weights, dimensions) keep Bengali numerals as before.
    - **Catalog screen** keeps the app-bar + bottom nav (it is a nav-level destination, so no back arrow) and gains a `.bar-actions` cluster on the right of `.bar-row`: 📦 → orders, 🛒 → cart with a gold `.cart-count` bubble. Search filters on name + brand; no match shows an `.empty-card`. Final layout, top to bottom: **promo slider → search + filter button → active-filter line → product grid**.
      - It originally opened with a `.comm-hero` moss title band (borrowed from Community) with the search lifted onto it. **The client asked for that band to be dropped and the slider put in its place** (*"Remove this section and add here slider"*) — so the slider is now the screen's opening element, the search sits below it at normal spacing keeping its raised shadow, and `.comm-hero` is once again used only by Community. Products now start ~200px higher up the page than they did with hero + chips + slider stacked above them.
    - **Category chips moved into a filter drawer** (user, after the catalog grew to 12 categories: *"these should on the drawer, add filter button right side of search input"*). Twelve chips wrapped to four rows on the page and pushed the products below the fold. The chips now live in a `.sheet` — the same bottom-sheet component as the Community ⋯ menu, not a centred `.modal-card`, which would read as a confirmation prompt — opened by a 🎚️ `.filter-btn` sitting beside the search input in a shared `.search-row`. Each chip carries its product count. Picking one applies and closes in a single tap.
      - With the chips hidden, two things had to replace what they used to show at a glance: the filter button wears a **gold dot** while a category is applied, and a `.filter-bar` above the grid shows the active category as a removable `.filter-tag` (✕ clears it) alongside a live "Nটি পণ্য" count. Without those, an applied filter is invisible and the grid just looks short.
      - Category filter and text search **compose** — searching "ND" inside ভ্যাকসিন narrows 6 → 2 rather than resetting the category.
    - **Promo slider at the top** (same request: *"it should be better if you add slider banner on the top"*). Four slides on a `scroll-snap` track with working dots and a 4.5s auto-advance — Home's `.banner-card` is a single static card whose label says "auto-scroll" but never did, so this is the first real one in the project. Manual swipes drive the dots and push the auto-advance back. **Each slide is an entry point, not decoration**: tapping one applies its category filter and scrolls to the grid (বাচ্চা / ভ্যাকসিন / ফিড, plus the free-delivery slide which clears the filter). Dots reuse the existing `.dot`/`.dot.active`. Slide height went 126 → 140px once it took over the top of the screen.
      - **The gold slide carries charcoal text, not white** (`.shop-slide.ink`). White on `--gold` measures ~1.9:1, nowhere near readable; charcoal on gold is ~7.8:1. This is the same call `.calc-btn.gold` already makes — **gold never carries white text anywhere in this app.** The moss, moss-dark and rust slides keep white (6.5:1 and better). The `ink` variant also swaps the decorative circle and emoji plate to white tints so they still read against gold.
    - **Detail** uses `.subpage-header.with-action` — the existing centred-title header with a right-hand action added, which needed `h1{margin-right:0}` and a 34px-wide back arrow so the title stays actually centred. The image gallery is a stand-in: the same emoji across three tinted swatches, since there is still no product photography. Sticky `.buy-bar` carries কার্টে যোগ করুন (gold) + এখনই কিনুন (moss, adds then jumps straight to checkout).
    - **Checkout** splits displayed data from inputs to stay inside the standing rule: the **saved address card is pre-filled** with realistic values (it's output), while the collapsible "নতুন ঠিকানা যোগ করুন" form below it is **empty placeholders** (it's input). District `<select>` lists the 8 divisions only — no invented upazila list.
    - **Order tracking** is a 5-stage rail (অর্ডার প্লেসড → নিশ্চিত হয়েছে → প্যাকিং চলছে → পাঠানো হয়েছে → ডেলিভার্ড). A cancelled order stops the rail where it actually got to and closes with a rust "অর্ডার বাতিল হয়েছে" step rather than pretending the remaining stages ran. Cancel is offered only before shipping (`stage <= 2`).
    - **`Shop.stars()` floors the rating and leaves the remainder hollow** (4.6 → ★★★★☆) with the number beside it — an earlier version rounded the half up and painted 4.6 as five solid stars, which read as a perfect score. There is no half-star glyph that renders reliably.
    - New CSS in full: `.tint-gold`/`.tint-rust`/`.tint-moss`/`.tint-indigo` (square emoji product tiles), `.bar-actions`/`.bar-icon`/`.cart-count`, `.subpage-header.with-action`, `.prod-grid`/`.prod-card`/`.prod-thumb`/`.prod-off`/`.prod-body`/`.prod-name`/`.prod-unit`/`.prod-price-row`/`.prod-price`/`.prod-price-old`/`.prod-add`/`.prod-link`, `.prod-hero`/`.prod-thumbs`/`.prod-thumb-mini`/`.prod-detail-head`/`.prod-brand`/`.prod-price-block`/`.prod-off-inline`, `.rating-row`/`.stars`/`.rating-count`, `.stock-tag` (+`.out`), `.qty-row`/`.qty-stepper` (+`.sm`)/`.qty-btn`/`.qty-val`, `.seller-card`/`.seller-info`/`.seller-name`/`.seller-loc`, `.hscroll`, `.cart-list`/`.cart-row`/`.cart-thumb`/`.cart-info`/`.cart-name`/`.cart-unit`/`.cart-line`/`.cart-remove`, `.calc-result-row.total`, `.buy-bar`, `.pay-list`/`.pay-option` (+`.on`)/`.pay-ico`/`.pay-body`/`.pay-name`/`.pay-sub`/`.pay-fee`, `.addr-card`/`.addr-body`/`.addr-name`/`.addr-text`/`.addr-toggle`, `.success-hero`/`.success-ring`/`.order-no`, `.order-card`/`.order-top`/`.order-id`/`.order-date`/`.order-items`/`.order-foot`, `.track-list`/`.track-step` (+`.done`/`.active`/`.cancelled`)/`.track-rail`/`.track-dot`/`.track-line`/`.track-text`/`.track-title`/`.track-when`, `.shop-search`, `.shop-section-title`, plus the slider/drawer pass: `.shop-slider`/`.slide-track`/`.shop-slide`/`.slide-circle`/`.slide-emoji`/`.slide-text`/`.slide-dots`, `.shop-slide.ink`, `.search-row`/`.filter-btn`(+`.on`)/`.fb-dot`, `.filter-bar`/`.filter-tag`/`.result-count`, `.filter-grid`/`.filter-chip .fc-count`, `.qty-row .qty-label small`. **No new tokens.** Everything else reuses `.search-bar`, `.filter-chip`, `.sheet-overlay`/`.sheet`/`.sheet-grip`/`.sheet-head`/`.sheet-cancel`, `.dot`/`.dot.active`, `.tabs`/`.tab`, `.calc-card`/`.card-title`, `.calc-results`/`.calc-result-row`, `.info-block`/`.info-row`, `.avatar`, `.post-verified`, `.tag-pill.status-*`, `.badge-circle`, `.empty-card`, `.notice`, `.modal-overlay`, `.toast`, `.action-stack`, `.calc-btn`, `.proj-card`.
    - Verified end-to-end in Chrome over `http://localhost`: 22 cards render, chip filter narrows to 4 feed items, a nonsense search shows the empty card, add-to-cart drives the badge 0→1→4, the detail stepper reaches 3 and adds 3, cart totals track qty changes, remove goes through the confirm modal, the free-delivery threshold flips ৳120 → ফ্রি at ৳5,000, express recalculates ৳110+৳250 = ৳360, placing an order clears the cart and lands on the right order number, the new order appears under চলমান beside the three seeded ones, tracking renders per stage, cancel moves it to the বাতিল tab and stops the rail, empty-cart states guard both cart and checkout, all `buy/` links and asset paths resolve, the six-item nav is intact, and no screen overflows horizontally.
    - Not built (no source, and out of the agreed scope): product reviews/ratings entry, wishlist, promo codes, real seller contact, search suggestions. **বেচুন (Sell) has since been built** (item 35), so the marketplace is no longer one-directional.

35. **বেচুন (Sell) — lead-capture section**, 8 pages under `sell/` plus `assets/js/sell.js`. `sell/index.html` (landing) · `pullet.html` · `chicken.html` · `egg.html` · `litter.html` (four category forms) · `success.html?id=` · `submissions.html` · `submission-detail.html?id=`.
    - **No recording exists for this section either** — same footing as Buy (item 34), designed from scratch on the established design system. The client spec was one sentence: a farmer offers **লেয়ার পুলেট / রেডি মুরগী / ডিম / লিটার**, submits a form, and an admin contacts him.
    - **This is lead capture, not a marketplace.** Nothing here prices, matches, publishes or lists a seller's stock publicly — the submission is the product, and the rep's callback is the next step. That framing is what keeps the section small: no seller-side pricing engine, no buyer↔seller messaging, no public listing catalog.
    - **Decisions taken with the user before building**: landing + one form page per category (rather than one page swapping field sets, or one generic form) · submission history **with** the status rail · inert photo drop-zone · confirm modal before submit · **expected price optional** per category unit, so a seller can skip it and let the rep quote · dropdown options restricted to vocabulary already in this codebase.
    - **Vocabulary discipline — nothing invented.** Breeds come only from names this app already uses: Hy-Line Brown/White, Novogen Brown/White, ISA Brown, সোনালী (pullet); ব্রয়লার, সোনালী, কালার বার্ড, Cobb 500, Ross 308, Indian River, Siam Broiler, লেয়ার (কালিং) (ready chicken); লেয়ার (বাদামি/সাদা), সোনালী, হ্যাচিং ডিম (egg — the last verified in `accounting/chick-quality.html`). Districts are the same 8 divisions `buy/checkout.html` uses. **লিটারের ধরন is a free-text input, not a `<select>`** — no litter-substrate vocabulary exists anywhere in this repo, and egg grade/size was left out entirely for the same reason. Both are one-line swaps to `<select>` once the client supplies real lists; each form's `.caption` says so.
    - **The four forms are genuinely different, not clones.** Pullet takes বয়স in weeks, ওজন in grams and a vaccine-schedule `.switch`; chicken takes বয়স in days, both গড় and মোট ওজন in kg (**typed, never computed** — the standing no-calculation rule applies, Buy's cart remains the single exception) plus a feed/medicine notes field; egg takes দৈনিক উৎপাদন alongside বিক্রয়যোগ্য পরিমাণ and a একবারে/নিয়মিত `.chip-select-row`; litter pairs a quantity with a বস্তা/কেজি/টন unit select and asks which flock it came from. Price units differ per category (৳/পিস · ৳/কেজি · ৳/১০০ পিস · ৳/বস্তা). Headers alternate moss (pullet, chicken) and rust (egg, litter).
    - **`assets/js/sell.js`** — the project's **third** shared JS file. Exposes one global, `Listings`, deliberately shaped like `Orders` in `shop.js`: `all`/`byId`/`byCat`/`place`/`cancel`/`statusOf`/`seed`/`newId`, plus form helpers `collect(cat, spec)`, `collectSeller()`, `summary(fields, keys, fallback)` and `sellerLine(seller)`. Storage key `eti_listings`; ids are `SELL-######`. **Loads after `shop.js` and reuses its helpers** (`readJSON`/`writeJSON`/`dateLabel`/`qs`/`toast`) rather than duplicating them — including the try/catch in-memory fallback, so the same "serve over http:// to see persistence" caveat applies. Do not call `Shop.paintBadge()` on sell pages; there is no cart badge in this section.
      - A listing stores its fields as an **ordered `[{label, value}]` array**, not a per-category object. That one choice is why `success.html` and `submission-detail.html` render all four categories without branching, and why adding a fifth category later needs no changes to either.
      - `collect()` drops blank fields, so a half-filled form never shows empty rows downstream; `Listings.CATEGORIES` is the single source for the landing cards, list thumbnails and labels.
      - `Listings.seed()` writes two demo submissions on first visit (one at যাচাই চলছে, one at বিক্রি সম্পন্ন) so আমার জমা is never blank — same rule the calculator result sections follow.
    - **Status rail is 4 stages** (জমা হয়েছে → যাচাই চলছে → প্রতিনিধি যোগাযোগ করেছেন → বিক্রি সম্পন্ন), reusing `.track-list`/`.track-step` verbatim from `buy/order-detail.html`. A cancelled submission stops where it got to and closes with a rust "জমা বাতিল হয়েছে" step. Cancel is offered only while `stage < 3` — once a rep has closed the sale there is nothing to call off. The list adds a **সব** tab ahead of চলমান/সম্পন্ন/বাতিল, which Buy's orders screen doesn't have.
    - **New CSS: one rule.** `.grid-label .grid-sub` — a second line inside a grid card's label, so each landing card states its price unit (প্রতি পিস / প্রতি কেজি / প্রতি ১০০ পিস / প্রতি বস্তা) before you open the form. Everything else reuses existing components: `.icon-grid`/`.grid-card`/`.grid-illustration`/`.badge-circle`, `.calc-card`, `.calc-input`(+`.calc-select`), `.form-label`, `.form-section-title`(+`.moss`/`.rust`)/`.sec-icon`, `.field-row`/`.fr-col`, `.chip-select-row`/`.chip-radio`/`.sr-only`, `.switch-row`/`.switch`, `.drop-zone`/`.dz-ico`, `.calc-btn`(+`.moss`/`.rust`/`.outline`), `.action-stack`, `.page-action`, `.modal-overlay`/`.modal-card`/`.modal-btn`, `.toast`, `.success-hero`/`.success-ring`/`.order-no`, `.order-card`/`.order-top`/`.order-items`/`.order-foot`, `.track-list`/`.track-step`, `.info-block`/`.info-row`, `.tag-pill.status-*`, `.tabs`/`.tab`, `.title-card`, `.proj-card`, `.empty-card`, `.notice`, `.shop-section-title`, `.caption`. **No new tokens.**
    - **No nav edits were needed** — `../sell/index.html` was already in the six-item bottom nav on all nine nav-carrying pages; the tab simply stopped landing on a placeholder. `sell/index.html` keeps the app-bar and bottom nav with no back arrow (nav-level destination); the other seven pages are drill-downs with `.subpage-header`/`.plain-header` back arrows and no bottom nav.
    - **Caption placement caught in verification**: captions were initially written inside `.screen-body`, where they render edge-to-edge against the phone frame. The repo convention is a **sibling of `.device`** — page-level footnote *under* the frame (as in `databank/disease-id-step4.html`). Fixed on all eight pages, including pulling them out of the two JS-rendered bodies.
    - Verified end-to-end in Chrome over `http://localhost`, **zero console errors**: all four forms submit through the confirm modal to the right `SELL-######`, each writing its own distinct field set (breed/weeks/grams · breed/days/kg · type/daily/supply · type/qty+unit/flock); blank optional fields drop out and an empty address renders "—"; the litter quantity+unit pair stitches back into one "140 বস্তা" row; success and detail render every captured value including the vaccine switch and both chip groups; the new submissions stack above the two seeded demos; সব/চলমান/সম্পন্ন/বাতিল all filter correctly and empty tabs show `.empty-card`; cancelling flips the pill to বাতিল, truncates the rail and removes the cancel button; persistence survives navigation and reload.
    - Not built (out of the agreed one-sentence scope): editing a submitted listing, uploading real photos, admin-side review, and any notification when a rep actually calls. (Seller accounts/login were also out of scope here; the account screens themselves landed later as item 36, though they are still not wired to Sell.)

36. **Auth — account entry screens**, 5 pages under `auth/` plus `assets/js/auth.js`. `auth/login.html` · `register.html` · `verify.html?flow=&id=` · `forgot-password.html` · `reset-password.html?id=`. Planned through OpenSpec (`openspec/changes/add-auth-screens/`) — proposal, two delta specs (`user-auth`, `password-recovery`), design and tasks.
    - **Designed, not transcribed.** No recording or screenshot folder exists for these screens — unlike everything before item 34, and on the same footing as Buy and Sell, they were designed from the established design system. The field set was kept deliberately minimal and conventional so there is little to unlearn if client frames turn up later.
    - **One identifier field, phone *or* email** (the user's call over a phone/email toggle or two separate fields). `Auth.isEmail()` sniffs a lone `@` and that only ever changes wording ("… ইমেইলে" vs "… মোবাইল নম্বরে") and the keyboard hint — it never validates, rejects or blocks input, per the standing "every field is typeable" rule.
    - **Standalone, gating nothing** (also the user's call). `index.html` and every section page are untouched: no redirect, no session check, no bottom-nav entry, no logout. Auth pages are reached by direct URL for now. Confirmed by diff — the change touches only `assets/css/styles.css`, plus the new `auth/` folder and `assets/js/auth.js`.
    - **`verify.html` is one file serving both flows**, not two near-identical files. `?flow=register|reset` sets the sub-line, the button label, the confirm destination (`../index.html` vs `reset-password.html`) and the back/change-identifier target (`register.html` vs `forgot-password.html`); an unknown or missing `flow` falls back to `register`, so a bare `verify.html` still renders a complete screen. The identifier rides along in `?id=` — same query-string mechanism `buy/` already uses, so every state stays directly openable for review (`verify.html?flow=reset&id=farmer@example.com`). Noted as a real-implementation caveat: an identifier in the URL belongs in a server-side session, not the address bar.
    - **`assets/js/auth.js`** — the project's **fourth** shared JS file, and the first that deliberately does **not** build on `shop.js`. It carries its own `qs`, `toast` and identifier sniffing (~15 duplicated lines) rather than loading 617 lines of catalog to read a query string, which would also drag `Shop`'s cart-badge painting and demo-order seeding onto pages that have neither. Exposes `qs`/`isEmail`/`idLabel`/`initIdField`/`bnNum`/`toast`/`togglePw`/`initOtp`/`initResend`. If a third consumer ever appears, the fix is a shared `util.js`, not another copy.
    - **The OTP row is six real `<input>`s** (`inputmode="numeric"`, `maxlength="1"`) wired to behave as one field: typing advances, backspace on an empty box retreats and clears, pasting fills left to right. Real inputs rather than a custom key-capture widget, so mobile keyboards stay predictable. The resend control is disabled behind a 60-second countdown (Bengali digits, `০:৫৯`) and, once enabled, only restarts the clock and shows a toast.
    - **Nothing authenticates.** No credential is checked, no code is sent or compared, no session is created, and nothing — not even a mock flag — is written to storage. Buttons navigate; the reset screen's success state is an in-page swap of the form card for a `.success-hero`, not a sixth file (a reset has no receipt to come back to). Each page carries a `.notice` saying so outright, since a demo viewer would otherwise report the missing validation as a bug.
    - Identifiers typed by the user are written with `document.createTextNode`/`textContent`, never `innerHTML` — same precedent as the community comment sheet.
    - New CSS only: `.auth-page`, `.auth-brand`/`.auth-logo`/`.auth-name`/`.auth-sub`, `.auth-title`/`.auth-lead`, `.pw-field`/`.pw-toggle`, `.otp-row`/`.otp-box` (+`.filled`), `.resend-line`, `.auth-link`, `.auth-alt`, `.auth-foot`. **No new tokens.** Everything else reuses `.calc-card`/`.calc-field`/`.calc-input`/`.calc-btn`/`.checklist-row`/`.notice`/`.toast`/`.success-hero`/`.plain-header`. The brand block replaces a colored header bar — a full-width `.subpage-header` would eat the top of a screen whose whole job is a short form.
    - Verified in Chrome over `http://localhost`, no console errors (one favicon 404 aside): both flows walk end-to-end (login → register → verify → Home, and login → forgot → verify → reset → success → login); the change-identifier link returns the typed value to the field prefilled; all three verify states (`register`, `reset`, bare) render correctly worded; OTP advance/backspace behave; the countdown enables the resend at zero, toasts and restarts; password reveal toggles both ways; mismatched reset passwords still succeed (by design); and `localStorage`/`sessionStorage` are empty after a full pass of both flows.
    - Not built: profile/account settings, logout, social sign-in, guest mode, and any entry point into auth from Home or the bottom nav — wiring auth into the app's navigation is a separate change.

## Screens NOT yet built (placeholders currently shown)
- Home / Hatchery **sub-screens** (Troubleshooting, Fine Tuning, Embryonic Stages, Important Topics, Management, Pull-Out Check detail pages — grid tab itself is built, drill-down pages are not)
- Accounting: **all 10 calculator/tool list items are now built, including the 4 breed sub-screens behind standard-data.html** ✅
- Buy (কিনুন): **full e-commerce flow now built** ✅ — catalog, product detail, cart, checkout, order confirmation, my orders, order tracking (see item 34). Designed from scratch, no recording; catalog contents are synthesized placeholders.
- Sell (বেচুন): **full lead-capture flow now built** ✅ — landing, four category forms (লেয়ার পুলেট / রেডি মুরগী / ডিম / লিটার), submission confirmation, my submissions, status tracking (see item 35). Designed from scratch, no recording. Still open: litter-type and egg-grade option lists (left as free text rather than invented), editing a submission, real photo upload, admin-side review.
- Auth: **login, registration, verification, forgot password and reset password are now built** ✅ (see item 36). Designed from scratch, no recording. Standalone — they gate nothing and nothing links to them yet. Still open: profile/account settings, logout, an entry point from Home or the bottom nav, and any real credential handling.
- **Doctor** (ডাক্তার) — appointment booking
- Data Bank: **all nine tools are now built, each with their full inner flow** ✅ (Flock Data Management, Vaccination Schedule, Lab Report, Disease Identification, Breeder Development Monitor, Feed Formulation, Molting Monitoring, Investment Calculator, Biosecurity Audit). Still unrecorded: Feed Formulation's **Result** action pill only — all four of its landing cards are built, including the Add/Edit-ingredient, Edit-formula and Nutrient Standard screens; Flock Data Management's **EDIT** screen, its **Completed** tab, and a Layer flock's daily entry (egg-count field implied, never opened); Vaccination Schedule's per-flock **OPEN** tracker (the day-by-day due/missed view scoped to one flock) and 14 of its ~18 demo schedule rows; Lab Report's `hh` flock card (only `hhvv` was opened), Rapid Test/Drug Sensitivity/Postmortem report forms (only Titer Report was opened), and Export/Share PDF (buttons exist, never tapped through); Disease Identification's disease-2 `জরুরি করণীয়` line and its image-upload picker; Biosecurity Audit's 4 unlinked Management Dashboard sub-screens (Repeated Findings, Comparison, Risk Trend Graph, Performance Ranking), 1 unlinked Reports & Compliance sub-screen (Critical Alerts — Previous Reports, PDF Reports, and CAPA Tracker are all now built), and **40 of the 60 checklist questions per audit form** (Farm/Hatchery each only ship a representative subset — see item 22).
- খামার ব্যবস্থাপনা / ভ্যাকসিন তথ্য / রোগ নির্ণয় / রোগ বালাই: **list pages are now built** ✅ — but each only has **one** article wired to a real detail page (শেড পরিষ্কার এবং জীবাণুমুক্তকরণ, ব্রংকাইটিস); the other ~20 article titles are static list-cards with no detail page yet — need more recordings or a decision to synthesize the rest
- Blog system (mentioned in original brief, no recording yet)

## Current file structure — multi-page static site, organized by folder
As the screen count grows, each top-level section gets its own folder (own `index.html` inside it) instead of piling flat files at the root. Shared CSS/JS/images live under `assets/`. **Root `index.html` is the single entry point** — that's what you open locally or point a static host at (Netlify Drop / Vercel / GitHub Pages all "just work" with zero config since routing is plain `<a href>` between real files, no server/build step needed).
```
eti-app/
├── index.html                       ← entry point / Home (General, Parent Stock, Hatchery tabs)
├── assets/
│   ├── css/styles.css                design tokens + component styles (shared by every page)
│   ├── js/app.js                     showSub() — toggles General/Parent Stock/Hatchery subpanels within index.html only
│   ├── js/shop.js                    Buy section: 36-product catalog + Cart + Orders (localStorage).
│   │                                 Sell also loads it, for Shop's money/date/qs/toast/storage helpers
│   ├── js/sell.js                    Sell section only: Listings + form collect helpers (localStorage).
│   │                                 Must load AFTER shop.js — it calls Shop.readJSON/dateLabel
│   ├── js/auth.js                    Auth section only: qs/toast/togglePw/initOtp/initResend.
│   │                                 Standalone — does NOT build on shop.js, and stores nothing
│   └── images/                       real images once available (currently using emoji placeholders)
├── auth/                             account entry (see item 36) — standalone, gates nothing
│   ├── login.html                    identifier (phone or email) + password, remember, forgot link
│   ├── register.html                 name, identifier, farm type, district, password ×2, terms
│   ├── verify.html                   6-box OTP + resend countdown, ?flow=register|reset&id=
│   ├── forgot-password.html          identifier entry → verify, ?id= prefill on return
│   └── reset-password.html           new password ×2 → in-page success state, ?id=
├── buy/                             full e-commerce flow (see item 34) — the only section with real cart logic
│   ├── index.html                    catalog — hero, search + filter drawer, promo slider, product grid
│   ├── product.html                  detail, ?id=<product>
│   ├── cart.html                     cart rows, qty steppers, live totals
│   ├── checkout.html                 address, delivery option, payment, order summary
│   ├── order-success.html            confirmation, ?id=<order>
│   ├── orders.html                   my orders — চলমান / সম্পন্ন / বাতিল tabs
│   └── order-detail.html             items + status timeline + cancel, ?id=<order>
├── sell/                             lead capture — submit stock, a rep calls back
│   ├── index.html                    landing — 4 category cards + "আমার জমা" entry
│   ├── pullet.html                   লেয়ার পুলেট form (breed, weeks, grams, vaccine switch)
│   ├── chicken.html                  রেডি মুরগী form (breed, days, avg + total kg)
│   ├── egg.html                      ডিম form (type, daily output, একবারে/নিয়মিত)
│   ├── litter.html                   লিটার form (free-text type, qty + unit, source flock)
│   ├── success.html                  submission confirmation, ?id=<listing>
│   ├── submissions.html              আমার জমা — সব / চলমান / সম্পন্ন / বাতিল tabs
│   └── submission-detail.html        fields + status rail + cancel, ?id=<listing>
├── doctor/index.html
├── community/                        6th bottom-nav tab, between ডাক্তার and ডাটা ব্যাংক
│   ├── index.html                    post feed + composer + ⋯ post menu
│   └── create-post.html              Create Post — audience pill, textarea, attach row
├── databank/
│   ├── index.html                    the 9-tool list
│   ├── flock-data-management.html    "Today Farm Dashboard" + the two flock-picker dialogs
│   ├── flock-create.html             Create New Flock form (Meat/Layer variants)
│   ├── flock-daily-entry.html        Daily Entry — Rearing Mode
│   ├── flock-list.html               Flock List, Edit and Delete (+ delete confirm)
│   ├── flock-report.html             Flock Report — monospace summary + table
│   ├── vaccination-schedule.html     nav-level landing: intro + Vaccine Calendar + due-today card
│   ├── vaccine-dashboard.html        Poultry Vaccine Dashboard (stat tiles + flock list)
│   ├── vaccine-flock-new.html        New Flock form (vaccination module)
│   ├── vaccine-flock-edit.html       Edit Flock form (vaccination module)
│   ├── vaccine-breed-schedule.html   Breed Wise Schedule + 3 chained modals
│   ├── molting-monitoring.html       Molting Management dashboard (stat tiles, empty flock list)
│   ├── molting-guide.html            Molting Protocol Guide — long-form article
│   ├── molting-add.html              Add Molting Flock form + CHECK DECISION result
│   ├── lab-report.html               Lab Report flock list + Add New Flock modal
│   ├── lab-report-detail.html        Flock detail + report-type/Add Titer Report modals
│   ├── lab-report-view.html          Titer report table
│   ├── lab-report-graph.html         Titer graph (Mean Titer / CV %)
│   ├── disease-id.html               Step 1 of the 5-step diagnosis wizard (Bird Type)
│   ├── disease-id-step2.html         Step 2 — Age Group
│   ├── disease-id-step3.html         Step 3 — সিম্পটম (28 checkboxes)
│   ├── disease-id-step4.html         Step 4 — Postmortem image upload
│   ├── disease-id-step5.html         Step 5 — Lesion (28 checkboxes)
│   ├── disease-id-result.html        Final Disease Analysis Result document
│   ├── feed-formulation.html         Feed Formulation landing — 4 tinted menu cards
│   ├── feed-select-type.html         Select Feed Type — 28-type grid + live search filter
│   ├── feed-formula-create.html      Formula builder — ingredient rows, nutrient readout, Save modal
│   ├── feed-ingredient-library.html  Ingredient Library — 15 ingredient cards + search
│   ├── feed-ingredient-add.html      Add / Edit Ingredient form
│   ├── feed-formula-history.html     Formula History — saved-formula card + Formula Details modal
│   ├── feed-formula-edit.html        Edit Feed Formula — prefilled, retitled, Save Changes
│   ├── feed-nutrient-standard.html   Nutrient Standard — 28 target cards + detail modal
│   ├── investment-dashboard.html     Investment Dashboard — stat tiles + saved project cards + ⋮ Edit/Delete
│   ├── investment-project-new.html   New Project form (7 farm types, 9 breeds, 64 districts)
│   ├── investment-project.html       Project hub — meta card + 7 planning tools + Export/Share PDF
│   ├── investment-fixed-capital.html Farm Area / Equipment & Automation / Fixed Capital Cost
│   ├── investment-working-capital.html Working Capital Auto Summary + Working Capital Cost
│   ├── investment-market-price.html  Editable Market Price — 15 unlabelled price defaults
│   ├── investment-mortality.html     Mortality Simulation — 12-row revenue scenario table
│   ├── investment-profit-roi.html    Profit and ROI Forecast — inputs + 4 result tiles
│   ├── investment-final-report.html  Final Investment Analysis + Bank Loan Estimation
│   ├── investment-cash-flow.html     Cash Flow Forecast — operational balance (gated at 0)
│   ├── breeder-monitor.html          Breeder Development Monitor — flock select + 8 collapsible report sections + 2 charts
│   ├── breeder-flock-create.html     Create Breeder Flock form
│   ├── breeder-weekly-input.html     Weekly Weight and Uniformity entry
│   ├── biosecurity-audit.html        Landing — Executive Dashboard stats + Management Dashboard/Reports & Compliance accordion
│   ├── biosecurity-executive-summary.html   Executive Summary report card
│   ├── biosecurity-audit-history.html       Farm/Hatchery audit history toggle + record
│   ├── biosecurity-farm-audit.html          Farm audit checklist (representative subset)
│   ├── biosecurity-hatchery-audit.html      Hatchery audit checklist (representative subset)
│   ├── biosecurity-previous-reports.html    Previous Reports list
│   ├── biosecurity-pdf-reports.html         PDF Reports list
│   ├── biosecurity-pdf-report.html          PDF report detail
│   └── capa-tracker.html             CAPA Tracker — filterable finding cards + Change Status modal

├── accounting/                       drill-down from Home's হিসাব নিকাশ grid card
│   ├── index.html                    the 10-tool list
│   ├── weight-uniformity.html        static mockup calculator (no working logic — by design)
│   ├── roi-fcr.html                  static mockup calculator (no working logic — by design)
│   ├── egg-mass.html                 static mockup calculator (no working logic — by design)
│   ├── breeder-feed.html             static mockup calculator (no working logic — by design)
│   ├── temp-converter.html           static mockup calculator (no working logic — by design)
│   ├── layer-egg-production.html     static mockup calculator (no working logic — by design)
│   ├── medicine-dosage.html          static mockup calculator (no working logic — by design)
│   ├── space-feeder-drinker.html     static mockup calculator (no working logic — by design)
│   ├── chick-quality.html            static mockup calculator (no working logic — by design)
│   ├── standard-data.html            sub-list menu (Broiler/Color/Layer/Duck), not a calculator
│   ├── standard-data-broiler.html    static mockup calculator (no working logic — by design)
│   ├── standard-data-color.html      static mockup calculator (no working logic — by design)
│   ├── standard-data-layer.html      static mockup calculator (no working logic — by design)
│   └── standard-data-duck.html       static mockup calculator (no working logic — by design)
├── production-cost/                  drill-down from Home's উৎপাদন খরচ grid card
│   ├── index.html                    the 3-tool list
│   ├── one-kg-cost.html              static mockup calculator (no working logic — by design)
│   ├── day-old-chick-cost.html       static mockup calculator (no working logic — by design)
│   └── egg-cost.html                 static mockup calculator (no working logic — by design)
├── farm-management/                  drill-down from Home's খামার ব্যবস্থাপনা grid card
│   ├── index.html                    6-item article list
│   └── shed-cleaning.html            full article detail (only one of 6 wired up — matches recording)
├── vaccine-info/                     drill-down from Home's ভ্যাকসিন তথ্য grid card
│   └── index.html                    6-item article list (no detail pages recorded yet)
├── disease-diagnosis/                drill-down from Home's রোগ নির্ণয় grid card
│   └── index.html                    6-item article list (no detail pages recorded yet)
└── disease-control/                   drill-down from Home's রোগ বালাই grid card
    ├── index.html                    6-item article list
    └── bronchitis.html               full article detail (only one of 6 wired up — matches recording)
```
**Conventions to keep following as more screens land:**
- Every bottom-nav-level screen gets `<section>/index.html` (mirrors `buy/`, `sell/`, etc.) — makes room for that section growing its own sub-pages later without a rename.
- A drill-down/detail screen reached from a grid card or list item lives as a sibling file inside the same folder as its parent list (e.g. all accounting calculator tools stay inside `accounting/`, next to `accounting/index.html`), not scattered elsewhere.
- CSS/JS are always referenced relative to the file's own depth: root pages use `assets/css/styles.css`; anything one folder deep uses `../assets/css/styles.css`.
- Bottom-nav pages link to each other with `../<section>/index.html`; the Home link from inside a section folder is `../index.html`.
- Each file still repeats the small shell markup (status-bar + app-bar + bottom-nav) itself — plain HTML has no includes without a build step, and this keeps every page a fully standalone, hostable file.
- Drill-down pages (like `accounting/index.html`) replace the app-bar with a colored `.subpage-header` + back arrow instead of showing the bottom nav, matching the recordings. Some tool screens instead use the plain `.plain-header` (no colored bar) — match whatever the recording actually shows, don't force one style onto both.
- **Every page except a bottom-nav destination gets a back arrow pointing at its real parent.** No exceptions — this rule replaces the earlier "keeps the bottom nav → the nav item is the way back, no arrow needed" reasoning, which the user rejected on sight. That reasoning was also wrong on its own terms: the nav item always goes to the *section index*, so on any screen more than one level deep (e.g. `lab-report-detail.html`, whose parent is `lab-report.html`) it silently skips the parent and the screen becomes a dead end. When the page has no header bar of its own, use a bare `.plain-header` with just the arrow and no `<h1>` — that renders as a lone moss `←` and sits fine under an app-bar. Only `<section>/index.html` files (Home, Buy, Sell, Doctor, Data Bank) legitimately have none.
- **"Static mockup, no working logic" means no calculation** — it does NOT mean no interactivity. Toggles that just switch which recorded UI state is visible (e.g. the ROI calculator's Broiler/Layer radio swapping field sets via `showFieldGroup()`) are fine and expected; the line is "shows another real state from the recording" (yes) vs. "computes a result from user input" (no).
- **Every field is typeable — no `disabled` on inputs.** This reverses the earlier convention. Text and number inputs used to carry `disabled` on the reasoning that typing implies a computed result; that was wrong, and the user hit it immediately ("I unable write anything in input field"). Typing is not computing: what we don't build is the *result*, not the ability to enter a value. A reviewer needs to type into a form mockup to judge it. `disabled` was stripped from all 16 tool screens across `accounting/` and `production-cost/` plus the Data Bank flow, and `.calc-input:focus` now draws the original's amber ring. **The only legitimate `disabled` is a field the original app itself renders greyed and locked** — currently just the pre-filled `21` in `flock-create.html`'s Layer form (`.calc-input:disabled` styles that case). `<select>`s were always enabled and stay that way (`class="calc-input calc-select"`).

## How to continue a new screen
1. User provides a screen recording of the feature.
2. Extract frames (ffmpeg), identify distinct states/screens. If ffmpeg is missing on a fresh machine, install per platform:
   - **macOS:** `brew install ffmpeg` (lands at `/opt/homebrew/bin/ffmpeg` on Apple Silicon)
   - **Windows:** `winget install --id Gyan.FFmpeg -e --silent`, then refresh PATH
3. Decide: is this a **new top-level bottom-nav screen** (new `<section>/index.html` folder, copy the shell from an existing page) or a **drill-down from an existing grid card/list item** (new file inside the parent section's folder, styled like `accounting/index.html`, linked via `<a class="grid-card" href="...">` or `<a class="list-card" href="...">`)?
4. Build functionality-match markup first, using the same CSS variables — don't invent new colors/fonts. If it's a calculator/tool screen, build the **static UI only** — no working computation — unless told otherwise. **Always fill the result section with realistic example output** (from the recording if it shows a computed result, otherwise a plausible synthesized one) — don't leave it blank/"Result will appear here"; every element needs to be visible for design review.
5. Wire up the link from wherever it's launched (bottom nav and/or a grid-card/list-card), double-checking relative path depth (`../assets/...` vs `assets/...`).
6. Apply the upgraded design treatment (badges, chips, shadows) once functionality is confirmed correct.
7. Test navigation by clicking through in browser before considering the screen done. Note: `file://` works fine for this site since there's no fetch/server dependency — just open `index.html` directly.

## Known gaps / open items
- Real product photography/illustrations needed to replace emoji placeholders (chicks, eggs, chicken breeds, vaccine vials, medicine bottles, etc.) — **most acute in `buy/`**, where every catalog card, the detail "gallery" and every cart row is an emoji tile standing in for a product photo
- Hatchery sub-screens unknown — need recordings *(Accounting and Data Bank are now complete)*
- Doctor/Blog still unbuilt — need recordings or specs. **Buy and Sell are both built from no source at all** (items 34 and 35). Buy's 36 products, prices, brands, sellers, delivery fees and payment options are all synthesized (the breed and vaccine *names* are reused from this app's own screens, but every price and quantity around them is invented) and need replacing with the client's real catalog and terms before this goes in front of anyone as factual. Sell holds the line tighter — every dropdown option there is vocabulary this repo already used — but it still needs the client's **litter-type and egg-grade/size lists**, which were deliberately left as free text rather than invented, plus confirmation of the "প্রতিনিধি ২৪ ঘণ্টার মধ্যে কল করবেন" callback window quoted on every form and the success screen
- **`buy/` needs `http://` for full behaviour** — `localStorage` is blocked on `file://` in Safari, so the cart won't persist across pages there (it degrades gracefully; pages still render). Every other screen in the project is fine over `file://`. Demo with `python3 -m http.server` or Chrome, or just deploy it
- Deployment: plan was Netlify Drop / Vercel / GitHub Pages once enough screens are done to show the client — current multi-page structure is already deploy-ready as-is, and hosting also removes the `file://` caveat above
